# 15챕터 최종 검수 지시서 (용어·문법·문체·구조)

검수 대상: `docs/ko/15-web-pages-with-giraffe.md` (707줄, 실행 단위 `15-view`·`15-escape`·`15-server`)
기준: `docs/ko/GLOSSARY.md`(334행 기준 병합본), `docs/ko/_pipeline/STYLE.md`
선행: `docs/ko/_pipeline/review/15-expert.md`, `docs/ko/_pipeline/review/batchD-glossary-merge.md`

게이트는 지금 상태에서 이미 통과한다(`check-note.sh` OK, 금지 패턴 0건). 아래는 게이트가 잡지 못하는
표기·문장·연쇄 정합성이다. 줄 번호는 참고용이며 STYLE.md 절차대로 문자열로 대상을 특정하라.

적용 순서 권고: 병합 보고서(4.1·4.2·4.5·5.1) → 전문가 지시서 → 이 지시서.
같은 자리를 건드리는 항목은 이 지시서가 우선이며, 그 자리마다 어느 지시를 흡수하는지 적어 두었다.

---

## 용어 일관성

- [15-web-pages-with-giraffe.md:38] 병기 누락 2건. 용어집 `web root`·`content root` 행이 15챕터를
  첫 등장으로 잡고 있는데 병기가 없다. 이 챕터의 병기는 지금 `정적 파일(static files)`(38줄)과
  `부분 뷰(partial view)`(259줄) 둘뿐이므로 중복 병기 위험도 없다.
  현재: "ASP.NET Core 의 기본 규칙은 콘텐츠 루트 아래 `wwwroot/` 를 웹 루트로 삼고 그 안의 파일만 내보내는 것이다."
  수정: "ASP.NET Core 의 기본 규칙은 콘텐츠 루트(content root) 아래 `wwwroot/` 를 웹 루트(web root)로 삼고 그 안의 파일만 내보내는 것이다."
  (같은 줄 뒤쪽 `콘텐츠 루트`·`웹 루트` 재등장 자리 48·515·541·684줄은 병기 없이 그대로 둔다.)

- [15-web-pages-with-giraffe.md:128] 병기 누락 2건 + 전문가의 `'` 보완을 한 번에 적용한다.
  용어집 `character reference`·`escape` 두 행이 모두 "15챕터 본론" 인데 병기가 어디에도 없다.
  `이스케이프` 는 절 제목(107줄)과 코드 주석(113줄)에만 나오므로 산문 첫 등장인 이 불릿이 병기 자리다.
  현재:
  ```text
  - 자식 자리든 특성 값이든 `str` 과 특성 함수를 지나면 `<`·`>`·`&`·`"` 가 문자 참조로 바뀐다. 그래서 특성 값에 따옴표가 들어와도 특성이 조기에 닫히지 않는다.
  ```
  수정:
  ```text
  - 자식 자리든 특성 값이든 `str` 과 특성 함수를 지나면 `<`·`>`·`&`·`"`·`'` 다섯 문자가 문자 참조(character reference)로 바뀐다. 이렇게 바꾸는 일을 이스케이프(escape)라 한다. 그래서 특성 값에 따옴표가 들어와도 특성이 조기에 닫히지 않고, 130-141줄 블록의 `&#39;` 가 홑따옴표가 바뀐 자리다.
  ```
  (마지막 절은 노트 자신의 출력과 본문을 잇는 것이므로 넣기를 권한다. 넣지 않겠다면 앞 두 문장만 쓰라.)

- [15-web-pages-with-giraffe.md:681] 정리 절의 같은 목록. 전문가 지시대로 `'` 를 더한다. 병기는 128줄에
  이미 했으므로 여기는 한국어만 쓴다.
  현재:
  ```text
  - `str` 과 특성 함수는 `<`·`>`·`&`·`"` 를 문자 참조로 바꾼다.
  ```
  수정:
  ```text
  - `str` 과 특성 함수는 `<`·`>`·`&`·`"`·`'` 다섯 문자를 문자 참조로 바꾼다.
  ```
  (뒤의 두 문장 "바깥에서 들어온 값을 ... 조각에만 쓴다." 는 그대로 둔다.)

- [15-web-pages-with-giraffe.md:34] 용어집 `deserialization` 행이 실측으로 좁혀 둔 서술 범위와 어긋난다.
  병합 4.4 가 같은 문장을 13챕터 2곳·14챕터 4곳에서 좁히는데 15챕터의 이 한 곳이 목록에서 빠졌다.
  세 챕터를 동시에 고치는 중이므로 이 줄만 옛 서술로 남는다.
  현재: `` `System.Text.Json` 이 판별 유니온을 다루지 못해 API 는 평평한 DTO 를 따로 두어야 했지만, ``
  수정: `` `System.Text.Json` 이 `Option` 이 아닌 판별 유니온을 다루지 못해 API 는 평평한 DTO 를 따로 두어야 했지만, ``
  (682줄의 "판별 유니온이 든 도메인 타입" 은 `HiveCode`·`InspectionId` 를 가리키는 자리이므로 그대로 둔다.)

- [15-web-pages-with-giraffe.md:691] 병합 5.1 이 15챕터의 `요청이 지나는 길` 을 1곳으로 세었으나 실제로는
  2곳이고, 전문가가 교체한 불릿(666줄) 말고 이 문단에 하나가 더 있다. 13챕터 (2)·14챕터 항목이 같은
  표현을 "미들웨어 파이프라인" 으로 바꾸므로 이 문단만 옛 표현으로 남는다. 아래 `구조` 절의 691줄
  전체 교체문에 반영해 두었다.
  현재: "요청이 지나는 길은 정적 파일 → 라우팅 → Giraffe 엔드포인트 →"
  수정: "미들웨어 파이프라인은 정적 파일 → 라우팅 → Giraffe 엔드포인트 →"

- [15-web-pages-with-giraffe.md:82] 같은 개념을 한 불릿 안에서 두 이름으로 부른다. 앞에서 용어집
  `element function` 행의 표기 `자식을 담을 수 없는 요소` 를 쓰고, 뒤에서 `닫히지 않는 태그` 로 바꿔 부른다.
  현재: "자식 목록을 붙이려 하면 컴파일되지 않으므로, 닫히지 않는 태그에 내용을 넣는 실수가 애초에 생기지 않는다."
  수정: "자식 목록을 붙이려 하면 컴파일되지 않으므로, 자식을 담을 수 없는 요소에 내용을 넣는 실수가 애초에 생기지 않는다."

- [15-web-pages-with-giraffe.md:451] 병합 4.5 의 교체문을 쓰라. 전문가 교체문에는 `내용 협상` 이
  들어 있어 용어집 `content negotiation`(콘텐츠 협상) 행을 위반한다. 적용 뒤
  `grep -c "내용 협상" docs/ko/15-web-pages-with-giraffe.md` 가 0 이어야 한다.
  (이 챕터의 유일한 출현이므로 이 한 곳을 놓치면 `콘텐츠 협상` 결정이 15챕터에 반영되지 않는다.
  병합문이 `14챕터에서 본 콘텐츠 협상` 으로 시작해 14챕터를 되짚는 형태이므로 병기는 필요 없다.)

- (선택) [15-web-pages-with-giraffe.md:3] `View Engine DSL` 의 `DSL` 이 병기 없이 약어로 나온다.
  용어집 `Domain-Specific Language (DSL)` 행은 첫 등장 병기를 규정한다. 13챕터를 읽고 온 독자를
  전제해 13챕터를 되짚는 문장이므로 그대로 두어도 되지만, 챕터별 독립 읽기를 살리려면 아래로 바꾼다.
  현재: "13챕터에서 훑어본 View Engine DSL 을 실제 화면 두 개에 쓰고,"
  수정: "13챕터에서 훑어본 View Engine 의 DSL 을 실제 화면 두 개에 쓰고,"

- (선택) [15-web-pages-with-giraffe.md:586-587, 593-594] 출력 문자열의 도메인 낱말이 산문과 갈린다.
  산문은 `여왕벌`(260·366·598줄), 검사 라벨은 `여왕을 본 기록`·`여왕을 못 본 기록` 이다.
  라벨을 `여왕벌을 본 기록`·`여왕벌을 못 본 기록` 으로 맞추고 기대 출력 주석 두 줄도 같이 고치면
  본문과 출력이 한 낱말을 쓴다. `printfn "  %-5b %s"` 는 라벨이 마지막 항목이므로 열 정렬은 깨지지 않는다.
  CSS 파일 안의 `content: " (여왕 확인)"` 는 화면 표시용이므로 그대로 두라.

- 확인만 하고 손대지 않는 것: `View Engine`(9곳)/`뷰 엔진`(2곳) 혼용은 병합 5.5 판정대로 정상이다.
  `마스터 페이지` 는 절 제목 `Adding a Master Page` 가 원어를 이미 보이므로 54줄에 병기를 더할 필요가 없고,
  용어집 `master page` 행이 요구한 "다른 뷰 엔진에서 마스터 페이지나 레이아웃이라 부르는 것" 잇기도
  54줄에 그대로 있다. `정적 파일 미들웨어`(4곳)·`부분 뷰`(16곳)·`공용 레이아웃`(8곳)·`컴프리헨션` 줄임형은
  모두 용어집 표기와 일치한다. 웹 맥락에서 `파이프라인` 단독 사용은 0곳이다.

---

## 문법·문체

- [15-web-pages-with-giraffe.md:3] 주술 불일치(STYLE 금지 패턴 2·"이 챕터는 ~ 넘어간다" 형). 챕터가
  화면을 내보내는 주체가 될 수 없다. `앱을 닫는다` 도 뜻이 흐리다.
  현재: "이 챕터는 그 API 가 다루는 데이터를 사람이 볼 화면으로 내보내며 앱을 닫는다."
  수정: "이 챕터에서는 그 API 가 다루는 데이터를 사람이 볼 화면으로 내보내며 앱을 마무리한다."

- [15-web-pages-with-giraffe.md:54] 띄어쓰기. `넘겨받다` 는 한 낱말이다.
  현재: "원서는 그 껍데기를 함수 하나로 뽑고 제목과 본문을 넘겨 받게 만든다."
  수정: "원서는 그 껍데기를 함수 하나로 뽑고 제목과 본문을 넘겨받게 만든다."

- [15-web-pages-with-giraffe.md:297] 주어가 없어 무엇이 무엇과 같아지는지 문장이 말하지 않는다.
  현재: "- `marks` 가 빈 리스트일 때 `li [] [ ... ]` 와 같아진다."
  수정: "- `marks` 가 빈 리스트면 `li marks [ ... ]` 는 `li [] [ ... ]` 와 같다."
  (뒤의 두 문장 "원서도 같은 방식으로 ... 실수하기 어렵다." 는 그대로 둔다.)

- [15-web-pages-with-giraffe.md:511] `값으로 확인된다` 가 무엇을 가리키는지 모호하다. 이 절에서 확인되는
  것은 값이 아니라 응답이다.
  현재: "- `configureApp` 의 순서가 곧 동작이라는 13챕터의 이야기가 여기서 값으로 확인된다."
  수정: "- `configureApp` 의 순서가 곧 동작이라는 13챕터의 이야기를 이 절이 실제 응답으로 확인한다."
  (뒤 문장 "`configureServices` 의 등록 순서는 상관없다." 는 그대로 둔다.)

- [15-web-pages-with-giraffe.md:540] 주격 조사가 한 절에 둘이라 주어가 겹친다.
  현재: "- CSS 의 `.queen-seen .code::after` 가 부분 뷰가 붙인 클래스를 받는다."
  수정: "- CSS 의 `.queen-seen .code::after` 는 부분 뷰가 붙인 클래스를 받는다."

- [15-web-pages-with-giraffe.md:572] 주어와 도구가 뒤바뀌었다. 주소창은 브라우저의 것이지 브라우저가
  주소창으로 여는 것이 아니다.
  현재: "브라우저가 주소창으로 이 경로를 열면 화면이 그려진다."
  수정: "브라우저 주소창에 이 경로를 넣으면 화면이 그려진다."

- [15-web-pages-with-giraffe.md:148] 전문가 196줄 항목의 (1) 안을 적용하면 이 산문만 옛 사실로 남는다.
  전문가 지시서에 없는 자리다. `#r` 과 복사 목록에서 어셈블리가 하나만 늘어나므로 "둘" 이 틀리게 된다.
  현재: "준비 코드도 14챕터의 것을 쓰되 정적 파일 미들웨어가 쓰는 어셈블리 둘을 참조 목록에 더한다."
  수정: "준비 코드도 14챕터의 것을 쓰되 정적 파일 미들웨어가 쓰는 어셈블리 하나를 참조 목록에 더한다."
  (전문가의 (2) 안, 즉 두 줄을 남기는 쪽을 고른다면 이 줄은 그대로 두라. (1) 을 권한 것과 짝을 맞춰라.)

- [15-web-pages-with-giraffe.md:298] 전문가 290줄 항목(특성 값 형식을 `yyyy-MM-ddTHH:mm:ssZ` 로 맞추기)을
  적용하면 이 산문이 옛 코드를 가리킨다. 전문가 지시서에 없는 자리다.
  현재:
  ```text
  특성 쪽에 `DateTime.ToString "s"` 로 정렬 가능한 형식을 넣고, 자식 쪽에 짧은 표기를 넣었다.
  ```
  수정(290 을 적용할 때): "특성 쪽에 `yyyy-MM-ddTHH:mm:ssZ` 로 정렬도 되고 시간대까지 드러나는 표기를 넣고, 자식 쪽에 짧은 표기를 넣었다. 14챕터의 `toPayload` 가 쓴 형식과 같으므로 같은 기록이 두 챕터에서 같은 문자열로 나간다."
  291줄 항목(`CultureInfo.InvariantCulture`)까지 적용한다면 같은 불릿 끝에 한 문장을 더하라.
  ```text
  짧은 표기는 불변 문화권으로 찍으므로 로캘이 달라도 `05-17 09:00` 이 그대로 나온다.
  ```
  두 항목을 모두 적용하지 않기로 하면 298줄은 그대로 두고, 전문가가 권한 "표시용 형식이라 시간대는
  담지 않았다" 한 줄을 이 불릿 끝에 붙여라.

- [15-web-pages-with-giraffe.md:598-599] 전문가가 검사 두 줄(`문서 선언`, `항목 순서`)을 더하라고 했으므로
  이 두 불릿의 개수 세기와 "마지막 줄" 지시가 모두 어긋난다. 전문가 지시서에 없는 자리다.
  검사 순서를 `문서 선언` → `레이아웃의 stylesheet` → `레이아웃의 script` → `목록 링크` → `건수` →
  `여왕을 본 기록` → `여왕을 못 본 기록` → `부분 뷰가 낸 시각` → `항목 순서` 로 두고(기대 출력 주석도
  같은 순서 아홉 줄), 두 불릿을 아래로 바꾼다. 서수 대신 라벨 이름으로 가리켜 뒤에 검사가 늘어도
  문장이 낡지 않게 했다.
  현재: "- 앞의 셋이 공용 레이아웃이 두 화면에 같은 껍데기를 씌웠다는 것을, 뒤의 넷이 부분 뷰와 컴프리헨션이 저장소의 세 건을 옮겼다는 것을 말한다. 여왕벌을 본 기록에만 클래스가 붙고 못 본 기록의 `<li>` 에는 특성이 없다는 차이도 확인된다."
  수정: "- `문서 선언` 은 `htmlView` 가 응답 앞에 `<!DOCTYPE html>` 을 붙였다는 것을, 이어지는 셋은 공용 레이아웃이 두 화면에 같은 껍데기를 씌웠다는 것을, `건수` 부터 `항목 순서` 까지 다섯은 부분 뷰와 컴프리헨션이 저장소의 세 건을 순서대로 옮겼다는 것을 말한다. 여왕벌을 본 기록에만 클래스가 붙고 못 본 기록의 `<li>` 에는 특성이 없다는 차이도 확인된다."
  현재: "- 표본 데이터의 기록 시각을 못박아 둔 덕에 마지막 줄처럼 시각까지 대조할 수 있다."
  수정: "- 표본 데이터의 기록 시각을 못박아 둔 덕에 `부분 뷰가 낸 시각` 검사처럼 시각까지 대조할 수 있다. `DateTime.UtcNow` 로 채웠다면 이 검사는 쓸 수 없다."
  이어서 전문가가 요구한 한계 불릿을 그 아래에 새로 넣어라.
  ```text
  - `body.Contains` 는 조각이 있는지만 본다. 항목 순서와 중첩은 `항목 순서` 검사처럼 이웃한 두 조각을 이어 붙여야 잡히고, 부분 뷰를 두 번 붙여 항목이 여섯이 되는 중복은 이 검사들이 잡지 못한다. 문서 전체의 모양은 `15-view` 단위가 문자열로 찍어 대조하는 쪽이 맡는다. 두 단위가 역할을 나눠 맡는다.
  ```
  `항목 순서` 검사를 넣지 않기로 하면, 위 두 문장에서 `` `건수` 부터 `항목 순서` 까지 다섯 `` →
  `` `건수` 부터 `부분 뷰가 낸 시각` 까지 넷 `` 로, 한계 불릿의 첫 문장 뒤는
"항목 순서와 중첩, 중복 출력은 잡히지 않고 문서 전체의 모양은 `15-view` 단위가 맡는다." 로 줄여라.
  라벨을 더해도 `printfn "  %-5b %s"` 는 불리언을 먼저 찍으므로 열 정렬은 깨지지 않는다.
  `get` 의 `%-26s` 도 새 경로가 없어 그대로다.

- [15-web-pages-with-giraffe.md:685] 전문가가 666줄의 기제 설명을 고치면 이 정리 불릿만 옛 기제
  ("먼저 적힌 쪽이 응답한다")로 남는다. 병합 5.1 의 15챕터 두 번째 교체(첫 문장의 "미들웨어 순서" →
  "미들웨어 파이프라인의 순서")도 이 줄이므로 아래 한 문장이 둘을 함께 흡수한다.
  현재: "- 미들웨어 순서는 취향이 아니다. 정적 파일 경로와 엔드포인트 경로가 겹치면 `configureApp` 에 먼저 적힌 쪽이 응답한다. 정적 파일을 앞에 두는 것이 관례다."
  수정: "- 미들웨어 파이프라인의 순서는 취향이 아니다. 정적 파일 경로와 엔드포인트 경로가 겹치면 `configureApp` 에 정적 파일을 먼저 적었을 때만 파일이 나간다. 정적 파일 미들웨어는 이미 골라진 엔드포인트가 있으면 요청을 그냥 넘기기 때문이다. 정적 파일을 앞에 두는 것이 관례다."

---

## 구조

- [15-web-pages-with-giraffe.md:5] 전문가의 도메인 나열 교정을 적용할 때 쓸 정확한 문장이다.
  현재: "도메인은 옥상 양봉장의 벌통 점검 기록이고 `HiveCode`·`Hive`·`InspectionId`·`Inspection` 이 있다."
  수정: "도메인은 옥상 양봉장의 벌통 점검 기록이고 `HiveCode`·`Hive`·`hives`·`findHive`·`InspectionId`·`Inspection` 이 있다."

- [15-web-pages-with-giraffe.md:691] 최종 모습 문단. 세 노트와 대조한 결과 사실은 맞고 세 곳이 어긋난다.
  (1) 5줄을 고치면 이 문단의 도메인 나열에 `hives` 가 빠져 노트 안에서 다시 갈린다(전문가 지시서에 없다).
  (2) 전문가가 지적한 경로 개수 표현과 `Program.fs` 열거.
  (3) 위 `용어 일관성` 의 `요청이 지나는 길`.
  덧붙여 첫 문장이 다섯 파일을 한 문장에 몰아 330자가 넘고 `과…과…와` 가 겹친다. 문단 전체를 아래로 바꿔라.
  현재(문단 전체): `` `HiveLog` 는 `dotnet new web -lang "F#"` 로 만든 단일 프로젝트이고 파일이 다섯이다. `Domain.fs` 가 … 다음 걸음이다. ``
  수정(문단 전체):
  ```text
  `HiveLog` 는 `dotnet new web -lang "F#"` 로 만든 단일 프로젝트이고 파일이 다섯이다. `Domain.fs` 가 `HiveCode`·`Hive`·`hives`·`findHive`·`InspectionId`·`Inspection` 을, `Store.fs` 가 `ConcurrentDictionary` 를 감싼 `InspectionStore` 를 담는다. `Inspections.fs` 에는 요청 타입·검증·API 핸들러 다섯 개와 `inspectionEndpoints` 가, `Views.fs` 에는 `pageLayout`·부분 뷰·`indexView`·`inspectionBoardView` 가, `Program.fs` 에는 화면 핸들러·요약 핸들러·벌통 핸들러·마지막 핸들러·라우팅·설정·진입점이 있다. 밖으로 드러난 경로는 세 갈래다. 화면 쪽에 `/` 와 `/inspections`, API 쪽에 `/api` 아래의 요약 하나와 벌통 조회 둘, 점검 기록 다섯(GET 두 개, POST, PUT, DELETE), 그리고 라우팅을 거치지 않는 `wwwroot/` 의 정적 파일이다. 미들웨어 파이프라인은 정적 파일 → 라우팅 → Giraffe 엔드포인트 → 어디에도 걸리지 않은 요청을 받는 핸들러 순이고, 서비스 등록은 라우팅·Giraffe·JSON 직렬화기·점검 기록 저장소 넷이다. 저장소 하나를 화면과 API 가 나눠 쓰므로 POST 로 만든 기록이 곧 화면에 나타난다. 데이터는 메모리에만 있어 프로세스가 끝나면 사라지고, 여기서 더 나아가려면 저장소 클래스 뒤에 실제 데이터베이스를 붙이는 것이 다음 걸음이다.
  ```
  대조 근거: 14챕터 918-920줄의 인계 문단과 14챕터 코드(`hiveListHandler`·`hiveStatusHandler`·
  `apiSummaryHandler`·`notFoundHandler` 가 `Program.fs`, 점검 기록 핸들러 다섯이 `Inspections.fs`),
  `apiEndpoints` 의 `route ""`·`/hives`·`routef "/hives/%s"` 와 `subRoute "/inspections"`.

- 절 제목·페이지·대조 표는 이상 없다. `##` 절 11개가 모두 `(원서 p.NN)` 또는 `(노트 보충)` 을 달고 있고,
  문서 끝 대조 표 11행의 절 이름·페이지가 제목과 한 글자도 다르지 않다(기계 대조 확인).
  실행 단위 `id` 도 표와 본문이 일치한다 — `15-view` 4블록, `15-escape` 2블록, `15-server` 14블록,
  `id` 없는 `fsharp` 펜스 1개(43-46줄, `.UseStaticFiles()` 조각). `](examples/...)` 링크나
  `docs/ko/examples` 경로는 없다. 코드 블록이 설명 사이사이에 놓여 있고 절 끝에 몰린 곳도 없다.

---

## 13·14챕터 동시 수정과 물리는 자리

세 챕터를 함께 고치는 중이므로 아래 다섯 곳은 15챕터 단독으로 판단하면 어긋난다.

1. [104줄 / 13챕터 424-428줄] 13챕터의 `hiveTableView` 가 `_href "/css/site.css"` 를 쓰고 15챕터가 만드는
   파일은 `hive.css` 다. 전문가가 두 갈래를 제시했으나 둘을 동시에 적용하면 15챕터 쪽 문장이 거짓이 된다.
   결정을 하나로 못박아라. 13챕터 예제 이름을 그대로 두는 쪽을 고르면 104줄 불릿 끝에 아래 한 절을 더한다.
   ```text
   13챕터의 표 예제가 링크한 `/css/site.css` 는 그 절의 예시일 뿐 앱의 파일이 아니었다.
   ```
   13챕터에서 이름을 `hive.css` 로 바꾸기로 하면 이 절을 넣지 마라(넣으면 없는 이름을 가리킨다).
2. [148·196줄] 전문가 196줄 (1) 안과 위 `문법·문체` 의 148줄 수정은 한 짝이다. 하나만 적용하면
   산문과 `#r` 목록이 어긋난다.
3. [34줄] 병합 4.4 의 판별 유니온 범위 좁히기가 13·14챕터만 대상이다. 15챕터 34줄을 함께 고쳐야
   세 챕터의 같은 문장이 같은 범위를 말한다(위 `용어 일관성` 항목).
4. [691줄 / 14챕터 920줄] 14챕터 인계 문단의 핸들러 이름 규칙이 병합 5.4 로 `<대상><동작 또는 응답 내용>Handler`
   로 바뀐다. 15챕터에는 이름 규칙을 다시 적은 자리가 없어 낡지 않는다(`grep` 확인). 691줄은 위 지시대로만 고치면 된다.
5. [148·152-193줄] 병합 4.1·4.2 의 준비 코드 교체문은 13·14·15 세 파일에서 바이트가 같다. 15챕터의
   152-193줄 블록이 그 대상이고, 15챕터에는 4.3 이 말한 주석·산문(`가장 높은 버전 폴더를 고른다`)이
   없으므로 코드만 바꾸면 낡는 서술이 없다(`grep` 확인). `printfn "ASP.NET Core %s"` 의 기대 출력도 그대로다.

---

## 선행 검수 지시서의 문제

전문가 교체문은 그대로 노트에 들어가므로 표기·문법을 함께 검수했다. 아래를 반영해 적으라.

- [전체] 전문가 교체문은 백틱이 없는 평문으로 적혀 있다. STYLE.md 문체 절이 F# 코드 식별자·연산자·
  타입 이름·오류 코드에 백틱을 요구하므로 노트에 옮길 때 백틱을 붙여야 한다. 붙일 대상:
  `List.length`, `Seq.length`, `for ... in`, `inspections: Inspection list -> XmlNode`,
  `inspections: Inspection seq -> XmlNode`, `unit -> InspectionStore`, `UseRouting`,
  `WebApplicationOptions(WebRootPath = "public")`, `builder.WebHost.UseWebRoot`,
  `System.NotSupportedException`, `Microsoft.AspNetCore.StaticFiles`, `UseStaticFiles`,
  `error FS0039`, `toPayload`, `myUL`·`myInput`·`checked`·`close`·`addBtn`, `Program.fs`,
  `open HiveLog`, `Views.`, `<!DOCTYPE html>`. (코드 블록 안 주석은 백틱을 붙이지 않는다 — 422·333줄
  교체문은 주석이므로 평문 그대로가 맞다.)

- [326줄 항목] 전문가안 첫 문장 `이 함수의 타입 주석은 없어도 같다` 는 무엇이 같은지 빠졌다.
  수정형: "- 이 함수의 타입 주석은 없어도 시그니처가 같다. 본문의 `List.length` 가 매개변수를 리스트로 못박기 때문에 주석을 지워도 FSI 가 `inspections: Inspection list -> XmlNode` 로 잡는다. 반대로 건수를 세는 줄을 빼거나 `Seq.length` 로 바꾸면 `for ... in` 이 요구하는 것은 열거 가능성뿐이라 `inspections: Inspection seq -> XmlNode` 가 된다. 제약은 컴프리헨션이 아니라 함께 쓴 함수가 정한다."

- [680줄 항목] 전문가안의 `그쪽이` 가 무엇을 가리키는지 흐리다.
  수정형: "타입 주석이 없으면 `for ... in` 이 요구하는 것은 열거 가능성뿐이라 매개변수가 `seq` 로 잡히고, 같은 본문에서 `List.length` 를 부르면 그 호출이 매개변수를 리스트로 못박는다."
  (불릿의 앞 두 문장 "목록은 자식 자리의 ... 결과는 같다." 는 그대로 두고 마지막 문장만 바꾼다.)

- [48줄 항목] 전문가안을 그대로 넣으면 지시대로 되지 않는다. 교체문 마지막 문장이 예외가 나는 방법으로
  끝나므로, 바로 뒤에 남는 `뒤의 검증 코드가 이 방법을 쓰는데` 의 `이 방법` 이 가장 가까운 선행 명사구
  (즉 통하지 않는 `builder.WebHost.UseWebRoot`)를 가리키게 된다. 지시어를 없애고 불릿 전체를 적어라.
  수정형(불릿 하나 전체):
  ```text
  - 웹 루트를 `wwwroot` 가 아닌 곳으로 바꾸려면 호스트를 만들 때 `WebApplicationOptions(WebRootPath = "public")` 처럼 옵션으로 넘긴다. 빌더를 만든 뒤에 `builder.WebHost.UseWebRoot` 로 바꾸려 하면 `System.NotSupportedException` 이 난다. 뒤의 검증 코드가 옵션으로 넘기는 쪽을 쓰는데, 스크립트로 실행할 때는 콘텐츠 루트가 프로젝트 폴더가 아니어서 경로를 못박아야 하기 때문이다.
  ```

- [451줄 항목] 전문가 교체문에 `내용 협상` 이 들어 있어 용어집 위반이다. 병합 4.5 의 15챕터 교체문
  (`14챕터에서 본 콘텐츠 협상으로 …`)을 쓰라. 전문가안을 쓰지 마라.

- [666줄 항목] 인과는 정확하다. 백틱만 붙이면 그대로 쓸 수 있다.
  수정형: "- 순서가 결과를 갈라 놓는 이유는 `UseRouting` 이 응답을 쓰지 않고 엔드포인트만 골라 둔다는 데 있다. 정적 파일 미들웨어는 이미 골라진 엔드포인트가 있으면 파일을 내보내지 않고 요청을 그냥 넘긴다. 그래서 라우팅을 먼저 켜면 경로가 겹치는 순간 엔드포인트가 이기고, 정적 파일을 먼저 두면 라우팅에 닿기 전에 파일로 응답이 끝난다. 겹치지 않는 경로라면 어느 순서든 파일이 나간다."
  이 불릿을 고치면 685줄이 옛 기제로 남는다(위 `문법·문체` 항목에서 함께 처리했다). 667줄
  ("적은 순서가 그대로 규칙이다")은 미들웨어 등록 일반에 대한 말이므로 그대로 두어도 어긋나지 않는다.

- [196줄 항목] (1) 안의 교체문에 백틱을 붙이고, 148줄 산문(`어셈블리 둘` → `어셈블리 하나`)을 함께 고쳐라.
  수정형: "- 새로 든 것은 `Microsoft.AspNetCore.StaticFiles` 하나다. `UseStaticFiles` 확장 메서드가 여기 들어 있고, 이 줄을 빼면 `error FS0039` 가 난다. 프로젝트에서는 프레임워크 참조에 들어 있어 적을 일이 없다."

- [625줄 항목] 교체문은 정확하다. 백틱만 붙여라.
  수정형: "- 마지막 줄이 14챕터의 목록 경로다. 화면을 더하는 동안 API 는 손대지 않았다. 다만 이 단위는 핸들러를 축약했으므로 필드가 둘뿐이고, 프로젝트에서는 14챕터의 `toPayload` 가 만든 다섯 필드가 그대로 나간다."

- [480줄 항목] 교체문이 정확하다. "- 시그니처가 `unit -> InspectionStore` 인 것에 뜻이 있다." 로 적고
  뒤 두 문장은 그대로 둔다. 462줄 코드 주석(`// FSI 실측: unit -> InspectionStore`)은 손대지 않아도
  이 문장과 어긋나지 않는다.

- [422줄 항목] 주석 교체문이 한 줄로 너무 길고(90자 이상), 요약·벌통 핸들러가 `Inspections.fs` 에
  있는 것처럼 읽힌다. 실제로 그 둘은 `Program.fs` 에 있다(14챕터 628·636·659줄). 691줄의
  `Program.fs` 열거와도 맞물리므로 아래처럼 세 줄로 적어라.
  ```
  // 14챕터의 점검 기록 핸들러 다섯 개 가운데 목록 하나만 남기고, 요약·벌통 핸들러와
  // subRoute "/inspections" 한 층도 접었다. 화면 쪽에 집중하기 위한 축약이고 프로젝트에서는
  // Inspections.fs 의 다섯 개와 Program.fs 의 요약·벌통 핸들러, 세 층 라우팅이 그대로 있다
  ```

- [39-40줄 항목] 부록 코드 경고 문장에 백틱과 `id` 표기를 넣어라.
  수정형(40줄 불릿 끝에 붙이는 한 문장): "부록 2 의 코드는 원서 뷰의 `id` 와 클래스 이름(`myUL`·`myInput`·`checked`·`close`·`addBtn`)을 그대로 찾으므로, 뷰에서 그 이름을 바꾸면 CSS 와 JavaScript 도 같이 바꿔야 한다."

- [396줄 항목] 추가 불릿에 백틱을 넣어라.
  수정형: "- 프로젝트에서는 `Program.fs` 에 `open HiveLog` 를 적어 `Views.` 접두를 남긴다. 14챕터가 엔드포인트 목록에 쓴 방식과 같다."

- [333줄 항목] 주석 교체문의 마지막 절을 평서형으로 맺어라(현재 `남겼다` 로 끝나 문제없으나 두 문장을
  한 줄에 이어 붙이면 길다). 두 줄로 적으라.
  ```
  // Domain.fs 의 타입 둘을 이 단위에서만 다시 적는다.
  // Inspection 은 뷰가 쓰는 필드만 남겼다
  ```

- [576-596줄 항목] 추가 검사와 기대 출력 주석의 배치는 위 `문법·문체` 의 598-599줄 항목에 순서까지
  적어 두었다. 두 지시가 같은 자리를 건드리므로 그쪽을 따르라.

- [원서 뷰 문구 불일치 항목] 교체문에 큰 문제는 없으나 `checked` 가 코드 이름이므로 백틱을 붙이고
  따옴표 표기를 통일하라.
  수정형: "- 원서는 같은 뷰를 두 번 싣는데 제목과 완료 표시가 서로 다르다(My To Do List / My ToDo List, `Read a book` 의 `checked` 여부, 여섯째 항목 이름). 화면이 책의 그림과 달라 보이는 것은 독자의 실수가 아니다."
