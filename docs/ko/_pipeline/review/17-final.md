# 17챕터 최종 검수 — 부록 1: VS Code 에서 솔루션과 프로젝트 만들기

검수 대상: `docs/ko/17-appendix-1-solution-setup.md` (222행, 실행 단위 1개)
기준: `docs/ko/GLOSSARY.md`(355행 표), `docs/ko/_pipeline/STYLE.md`
선행: `docs/ko/_pipeline/review/17-expert.md`, `docs/ko/_pipeline/review/batchE-glossary-merge.md`
대조: `docs/ko/04-organising-code-and-testing.md`

기술 내용은 재검토하지 않았다. 아래는 용어 표기·한국어 문법·문체·구조와,
선행 지시서가 노트에 넣게 될 교체 문장의 품질만 다룬다.

적용 순서 권고: 먼저 병합 보고서 4·5·6절과 전문가 지시서를 적용하고, 그 다음에 이 문서를
적용하라. 이 문서의 "선행 검수 지시서의 문제" 절은 선행 교체문을 그대로 넣지 말고
여기 적은 수정형으로 넣으라는 지시다(STYLE.md 126행 — 같은 곳을 건드리면 최종 검수가 우선).

먼저 확인한 것(고칠 것 없음)

- 펜스 13개, 열기 펜스만 세면 `bash` 5·`xml` 4·언어 표시 없는 출력 인용 2·`fsharp` 2 다.
  전문가의 계수 정정("`xml` 5개" 가 실제 4개)이 맞다. 노트에 고칠 것은 없다.
- 실행 단위 `id` 는 `17-shipping` 하나뿐이고 본문(136행)과 대조 표(221행)가 일치한다.
- `id` 없는 `fsharp` 블록(167행)은 정상이다. 절 제목의 `(원서 p.NN)`·`(노트 보충)` 표기가
  모든 절에 있고 대조 표 여섯 행과 절 제목이 글자까지 일치한다.
- `](examples/` 나 `docs/ko/examples` 경로는 없다. `check-note.sh` 금지 패턴 위반 없음.
- 본문의 노트 파일 이름은 3·19행 두 곳이 전부이고 둘 다 병합 보고서 17-D·17-E 가 없앤다.
  적용 뒤에는 파일 이름 참조가 0 이 된다. 병기가 두 곳에 남는 자리는 없다(노트 전체에 병기 0개).
- `17-shipping` 블록은 이 문서의 어느 수정도 건드리지 않는다. `printfn "%-7s %d"` 의 라벨이
  `0.4kg`·`3.0kg`·`7.2kg` 로 모두 다섯 글자이므로 열 정렬은 그대로다.
- "솔루션 디렉터리"(46·116행)와 "솔루션 폴더"(78·209행)를 용어집 `solution folder` 행의
  요구대로 갈라 쓰고 있다. 아래 [17:15]·[17:26] 만 예외다.

---

## 용어 일관성

- [17:65] `원서 시절의 `.sln` 형식이 필요하면` → `예전 `.sln` 형식이 필요하면`
  (용어집 `solution file` 행이 "`-f sln` 을 주면 예전 `.sln` 이 나온다" 로 적고,
  04챕터 50행도 "예전 `.sln` 형식이 필요하면" 이다. "원서 시절의" 는 이 노트에만 있는 표기다.)

- [17:66] `프로젝트 템플릿의 짧은 이름은` → `템플릿 짧은 이름은`
  (용어집 `template short name` 행의 확정 표기가 "템플릿 짧은 이름" 이다. 같은 행이 정의를
  "`dotnet new list` 출력의 Short Name 칸 값" 으로 잡으므로 문장도 그 칸을 가리키게 맞춘다.)
  현재: `- 프로젝트 템플릿의 짧은 이름은 `dotnet new list --language "F#"` 으로 확인할 수 있다. 이 부록에서 쓰는 것은 `console` 과 `xunit` 이다.`
  수정: `- 템플릿 짧은 이름은 `dotnet new list --language "F#"` 출력의 Short Name 칸에서 확인한다. 이 부록에서 쓰는 것은 `console` 과 `xunit` 이다.`

- [17:15] 디스크의 디렉터리를 "폴더" 로 부른 자리다. 9·46·68·116·206행은 전부 "디렉터리" 이고,
  용어집 `solution folder` 행은 솔루션 폴더가 "디스크의 디렉터리" 가 아니라는 점을 못박아 두었다.
  디스크 쪽은 "디렉터리", VS Code 가 열어 두는 대상만 "작업 폴더" 로 갈라 쓴다.
  현재: `- 빈 폴더를 열고 시작한다. 폴더 안에서 `code .` 로 열면 그 폴더가 작업 폴더가 된다.`
  수정: `- 빈 디렉터리에서 시작한다. 그 디렉터리에서 `code .` 로 열면 거기가 VS Code 의 작업 폴더가 된다.`

- [17:26] 같은 이유로 코드 주석도 맞춘다(주석의 한국어는 검수 대상이다).
  현재: `# setup.sh — 빈 폴더에서 bash setup.sh 로 실행한다`
  수정: `# setup.sh — 빈 디렉터리에서 bash setup.sh 로 실행한다`
  (주석 폭이 바뀌지만 이 블록에는 정렬을 맞춘 열이 없어 영향이 없다.)

- [17:92] 46행 교체문이 "스크립트를 부분적으로 골라 다시 돌리면" 을 쓰게 되므로 같은 사실을
  가리키는 92행의 표현을 맞춘다.
  현재: `스크립트를 부분적으로 다시 돌려도 이 명령은 걸림돌이 되지 않는다.`
  수정: `스크립트를 부분적으로 골라 다시 돌려도 이 명령은 걸림돌이 되지 않는다.`

- 병기(판단 필요, 아래 두 곳을 함께 고치거나 둘 다 그대로 두어라). 이 노트에는 병기가 하나도 없다.
  16챕터도 0개이므로 그 자체로 위반은 아니다. 다만 이 부록은 04챕터가 본문에서 가리키는 곳이라
  따로 펼쳐 읽힐 자리이고, 04챕터는 같은 낱말 셋을 모두 병기해 두었다(04:3 솔루션·프로젝트,
  04:23 어서션). 병기를 넣기로 하면 이 세 낱말에만 넣어라.
  - [17:3] 현재: `원서 4챕터(p.52)는 솔루션과 프로젝트를 먼저 만들어 두라고만 하고`
    수정: `원서 4챕터(p.52)는 솔루션(solution)과 프로젝트(project)를 먼저 만들어 두라고만 하고`
  - [17:94] 현재: `테스트 프로젝트에 참조와 어서션 라이브러리를 붙인다.`
    수정: `테스트 프로젝트에 참조와 어서션(assertion) 라이브러리를 붙인다.`
  - 나머지 낱말(통합 터미널·명령 팔레트·대상 프레임워크·로케일·BOM·CLI·SDK)에는 넣지 마라.
    용어집이 이 낱말들에 병기를 요구하지 않고, 일부에만 병기가 붙으면 오히려 기준이 흐려진다.

---

## 문법·문체

- [17:7] 주술 불일치. 주어가 "구조는" 인데 서술어가 "둔다" 로 넘어간다.
  현재: `- 원서가 만드는 구조는 솔루션 하나에 프로젝트 둘이다. 코드용 콘솔 프로젝트와 테스트용 xUnit 프로젝트이며, 각각 `src/` 와 `tests/` 아래에 둔다.`
  수정: `- 원서가 만드는 구조는 솔루션 하나에 프로젝트 둘이다. 코드용 콘솔 프로젝트와 테스트용 xUnit 프로젝트를 각각 `src/` 와 `tests/` 아래에 둔다.`

- [17:18] "보내는 것은 … 항목이거나 … 고르는 것이다" 로 주술이 어긋나고 한 문장이 146자다.
  현재: `- 선택한 텍스트를 터미널로 보내는 것은 Terminal 메뉴의 Run Selected Text 항목이거나, `CTRL+SHIFT+P` 로 명령 팔레트를 열고 Terminal: Run Selected Text in Active Terminal 을 고르는 것이다.`
  수정: `- 선택한 텍스트를 터미널로 보내는 길은 둘이다. Terminal 메뉴의 Run Selected Text 항목을 고르거나, `CTRL+SHIFT+P` 로 명령 팔레트를 열고 Terminal: Run Selected Text in Active Terminal 을 고른다.`

- [17:44] 주어가 없어 "헷갈리지 않는다" 의 임자가 흐리다.
  현재: `스크립트가 끝난 뒤 셸이 어디에 서 있는지 헷갈리지 않는다.`
  수정: `스크립트가 끝난 뒤 셸이 어디에 서 있는지 헷갈릴 일이 없다.`

- [17:45] "실행 후" 를 풀어 쓴다.
  현재: `- 스크립트를 파일로 만들었다면 실행 후 지워도 된다.`
  수정: `- 스크립트를 파일로 만들었다면 실행한 뒤 지워도 된다.`

- [17:68] 한 문장에서 "만들고 … 만들어" 가 겹친다.
  현재: `디렉터리 둘을 만들고 프로젝트 둘을 만들어 솔루션에 넣는다.`
  수정: `디렉터리 둘과 프로젝트 둘을 만들어 솔루션에 넣는다.`

- [17:124] 주술 불일치("테스트는 … 하나만 잡힌다").
  현재: `- 테스트는 xUnit 템플릿이 만들어 둔 `My test` 하나만 잡힌다. 여기까지 오면 환경이 완성된 것이다.`
  수정: `- 잡히는 테스트는 xUnit 템플릿이 만들어 둔 `My test` 하나다. 여기까지 오면 환경이 완성된 것이다.`

- [17:134] "넣을 가장 작은 대상" 이 무엇을 가리키는지 흐리다.
  현재: `아래 함수는 그 자리에 넣을 가장 작은 대상이다.`
  수정: `아래 함수는 그 자리에 넣어 볼 가장 작은 예다.`

- [17:151] 한 문장에 인과가 셋 겹쳐 읽기 힘들고 피동이 끝에 붙는다.
  현재: `- 마지막 값이 6400 인 이유는 5kg 초과분 2.2kg 을 `ceil` 로 3kg 으로 올려 800 을 곱한 2400 이 기본 요금 4000 에 더해지기 때문이다.`
  수정: `- 마지막 값은 이렇게 나온다. 5kg 초과분 2.2kg 을 `ceil` 로 3kg 으로 올리고 800 을 곱해 2400 을 얻은 뒤, 기본 요금 4000 에 더한다.`

- [17:163] "이 부분과" 가 무엇인지 흐리다.
  현재: `이 부분과 컴파일 순서 규칙은 4챕터 노트에서 다뤘다.`
  수정: `이 기능과 컴파일 순서 규칙은 4챕터 노트에서 다뤘다.`

- [17:186] 불필요한 피동.
  현재: `여기까지 확인되면 부록의 역할은 끝이다.`
  수정: `여기까지 확인했으면 부록의 역할은 끝이다.`

- [17:17] (권장) 18행이 메뉴·명령 이름을 원어로 적어 주므로 이 줄도 명령 이름을 적어 주면
  독자가 편집기에서 찾을 수 있다.
  현재: `선택한 낱말과 같은 낱말 전부를 동시에 편집하는 기능이다.`
  수정: `선택한 낱말과 같은 낱말 전부를 동시에 편집하는 Change All Occurrences 기능이다.`

원문 의존도: 직역투나 원서 문장 구조가 그대로 드러난 문장은 없다. 원서 절차를 요약하고
실측한 명령·출력으로 다시 짠 서술이다. 셸 명령과 CLI 출력 인용은 원문 그대로 두었다.

---

## 구조

- [17:78 뒤] 병합 보고서 17-B 가 더하라고 한 불릿의 자리를 옮겨라. 78행 불릿은 마지막 문장이
  "넣고 나면 `.slnx` 가 이렇게 채워진다" 로 바로 아래 80-89행 XML 블록을 가리킨다. 그 사이에
  새 불릿을 끼우면 블록을 가리키는 문장과 블록이 갈라진다.
  넣을 자리: 91행 불릿("이 폴더 항목은 편집기의 솔루션 탐색기에 …") 바로 다음, 92행
  불릿("등록 결과는 `dotnet sln list` 로 …") 앞이다. 폴더 이야기가 한자리에 모인다.

- [17:194] 병합 보고서 17-M 이 붙이라고 한 표 앞머리 문장을 그대로 이어 붙이면 "아래는" 으로
  시작하는 문장이 두 개 나란히 온다. 단락째로 갈아라.
  현재: `원서는 2023년 1월 판이고 아래는 SDK 10.0.111 에서 실측한 결과다. 명령 이름과 순서는 원서 그대로 유효하다.`
  수정: `원서는 2023년 1월 판이고 아래 표는 SDK 10.0.111 에서 실측한 결과다. 원서 이후 달라진 것과 이 노트가 달리 잡은 것을 함께 적었다. 명령 이름과 순서는 원서 그대로 유효하다.`

- [17:103 뒤] 병합 보고서 17-F 가 "블록 뒤 불릿에 한 줄" 이라고만 적었으므로 문장을 확정해 둔다.
  113행 불릿 앞에 이 불릿을 넣어라.
  `- 블록에 적힌 템플릿 패키지 버전은 SDK 10.0.111 의 xUnit 템플릿 기준이다.`
  (202행 표 행을 지우고도 버전 정보가 남는다.)

- [17:192, 17:221] 절 제목 `## 원서와 달라진 점 (노트 보충)` 은 그대로 두어라. 표가 이 노트의
  편집 판단까지 담게 되니 제목을 넓히고 싶어질 수 있는데, 제목을 고치면 대조 표 221행
  `| 원서와 달라진 점 | (노트 보충) | — |` 도 함께 고쳐야 한다. 앞머리 단락으로 범위를 밝히는
  위 수정으로 충분하다.

- 정리 절의 불릿 여섯 개는 순서와 대응이 맞다. 206행 열거는 17-I 적용 뒤 "여덟 단계" 와
  항목 수가 정확히 맞는다(솔루션 만들기·디렉터리 둘 만들기·프로젝트 둘 만들기·솔루션에
  넣기·참조 걸기·패키지 넣기·빌드·테스트 = 8).

---

## 선행 검수 지시서의 문제

전문가·병합자가 제시한 교체문 가운데 그대로 넣으면 안 되는 것이다. 아래 수정형으로 넣어라.

- [전문가 17:16 / 병합 6.2 첫째 항목] 교체문 셋째 문장의 "표기는 … 따옴표다" 가 주술이 어긋난다.
  전문가안: `- 통합 터미널을 새로 연다. VS Code 의 기본값은 `CTRL+SHIFT+백틱` 이고, 이미 열린 패널을 접었다 펴는 것은 `CTRL+백틱` 이다. 원서 표기는 `CTRL+SHIFT+'` 로 백틱이 아닌 따옴표다. 원서가 어느 키를 가리켰는지는 확인할 방법이 없으니 편집기 기본값을 따르면 된다.`
  수정형: `- 통합 터미널을 새로 연다. VS Code 의 기본값은 `CTRL+SHIFT+백틱` 이고, 이미 열린 패널을 접었다 펴는 것은 `CTRL+백틱` 이다. 원서가 적은 것은 `CTRL+SHIFT+'` 로 백틱이 아니라 따옴표다. 원서가 어느 키를 가리켰는지는 확인할 방법이 없으니 편집기 기본값을 따르면 된다.`

- [17:13] 위 교체문을 적용하면 절 앞머리가 낡는다(전문가 지시서에 없는 파급이다). 지금 13행은
  "괄호 안은 현재 VS Code 문서의 기본 단축키다" 라고 하는데, 교체문이 괄호를 없애고 17·18행의
  단축키는 애초에 괄호 밖에 있다. 앞머리를 갈아 확인된 것과 원서 서술을 갈라 주어라.
  현재: `아래 항목은 편집기 기능이라 셸에서 확인할 수 없다. 원서 기준 서술이고, 괄호 안은 현재 VS Code 문서의 기본 단축키다.`
  수정: `아래 항목은 편집기 기능이라 셸에서 확인할 수 없다. 절차는 원서를 따라 적었고, 적어 둔 단축키는 현재 VS Code 문서의 기본값이다.`

- [전문가 17:23 경고 한 줄] 같은 지시서의 [17:23, 17:207] 항목이 "첫 줄의" → "맨 앞의" 로 고치라고
  하는데, 새로 넣는 경고문이 다시 "이 첫 줄은" 이라고 부른다(블록 첫 줄은 주석이다). 게다가
  23행은 단락이고 그 아래가 바로 코드 블록이라, 불릿 하나를 끼우면 단락과 블록 사이에 목록이
  끼어든다. 단락에 문장으로 붙여라.
  전문가안: `- 이 첫 줄은 파일에 넣어 `bash setup.sh` 로 돌릴 때만 쓴다. 통합 터미널에 골라 보내면 뒤이어 실패하는 명령 하나에 그 터미널이 닫힌다.`
  수정형(23행 단락 전체를 이렇게 갈아라):
  `원서처럼 명령을 파일에 모아 두는 방식을 쓴다면 확장자를 `.txt` 대신 `.sh` 로 잡고 셸에 파일째로 넘기는 편이 안전하다. 선택 텍스트를 터미널로 보내는 방식은 앞 명령이 실패해도 뒤 명령이 계속 실행되기 때문이다. 맨 앞의 `set -euo pipefail` 이 그 문제를 막는다. 다만 이 줄은 파일에 넣어 `bash setup.sh` 로 돌릴 때만 쓴다. 통합 터미널에 골라 보내면 뒤이어 실패하는 명령 하나에 그 터미널이 닫힌다.`
  (이 교체 하나로 [17:23] 의 "첫 줄의"→"맨 앞의" 수정까지 함께 처리된다. 207행의
  "`set -euo pipefail` 을 첫 줄에 두고" → "맨 앞에 두고" 는 따로 적용하라.)

- [전문가 17:46 / 병합 6.2 셋째 항목] 셋째 문장에 주어가 없어 무엇이 진행하는지 읽히지 않고,
  "걸린다" 가 무슨 일인지 모호하다.
  전문가안 뒷부분: `메시지가 안내하는 `--force` 를 붙이면 덮어쓰기까지 진행한다. 스크립트를 부분적으로 골라 다시 돌리면 `mkdir src tests` 도 걸린다.`
  수정형(불릿 전체):
  `- 이 스크립트는 같은 자리에서 두 번 돌릴 수 없다. `dotnet new sln` 이 기존 `.slnx` 를 덮어쓰겠다는 확인을 요구하며 종료 코드 73 으로 멈추고, `set -e` 때문에 뒤 명령은 실행되지 않는다. 메시지가 안내하는 `--force` 를 붙이면 `dotnet new sln` 이 기존 파일을 덮어쓰고 넘어간다. 스크립트를 부분적으로 골라 다시 돌리면 `mkdir src tests` 도 실패한다. 다시 만들 때는 솔루션 디렉터리를 지우고 처음부터 돌리는 것이 깔끔하다.`

- [병합 5.3 17-F 교체문 1] `<ItemGroup>` 을 주제로 두 번 세우고 둘째에만 "은" 을 붙여 주술이
  늘어진다.
  병합안: `- 두 명령을 돌린 뒤 `tests/ShopTests/ShopTests.fsproj` 의 패키지 `<ItemGroup>` 과 새로 생긴 프로젝트 참조 `<ItemGroup>` 은 이렇게 된다. …`
  수정형: `- 두 명령을 돌린 뒤 `tests/ShopTests/ShopTests.fsproj` 에서 패키지 `<ItemGroup>` 과 프로젝트 참조 `<ItemGroup>` 이 이렇게 된다. `FsUnit.xUnit` 은 템플릿이 만들어 둔 패키지 `<ItemGroup>` 안에 이름 순으로 끼어들고, 새 `<ItemGroup>` 을 얻는 것은 프로젝트 참조 쪽뿐이다. 파일 앞쪽의 `<Compile Include="Tests.fs" />` 항목은 그대로다.`
  (XML 블록 교체문 자체는 손댈 것이 없다.)

- [병합 5.3 17-G / 전문가 17:153] "부를 생각이면" 이 문어체에서 벗어나고, 넷째 문장이 100자를
  넘겨 한 호흡에 읽히지 않는다.
  병합안: `… `Program.fs` 에서 `feeFor` 를 부를 생각이면 정의가 앞에 와야 하고, 부르지 않아도 순서를 뒤집으면 빌드가 실패한다. 템플릿이 만든 `Program.fs` 에는 모듈 선언이 없고 선언을 생략할 수 있는 파일은 마지막 하나뿐이라, `Shipping.fs` 를 뒤에 적으면 오류 FS0222 가 난다. 이 규칙은 4챕터 노트에 있다.`
  수정형(문단 전체):
  `이 코드를 `src/Shop/Shipping.fs` 로 저장했다면 `Shop.fsproj` 의 컴파일 목록에 적어야 한다. 자리는 `Program.fs` 앞이다. `Program.fs` 에서 `feeFor` 를 부른다면 정의가 앞에 와야 하고, 부르지 않아도 순서를 뒤집으면 빌드가 실패한다. 템플릿이 만든 `Program.fs` 에는 모듈 선언이 없다. 선언을 생략할 수 있는 파일은 마지막 하나뿐이므로 `Shipping.fs` 를 뒤에 적으면 오류 FS0222 가 난다. 이 규칙은 4챕터 노트에 있다.`

- [병합 5.3 17-H / 전문가 17:162] "걸리는 것은 … 때다" 가 주술 불일치다.
  병합안 셋째 문장: `걸리는 것은 스크립트로 치환할 때다.`
  수정형(불릿 전체):
  `- 템플릿이 만든 `.fsproj` 와 `.fs` 는 BOM 이 붙은 UTF-8 이다(`.slnx` 는 BOM 이 없고 `-f sln` 으로 만든 `.sln` 에는 있다). BOM 을 떼도 빌드는 성공하므로 인코딩 자체가 문제가 되지는 않는다. 걸리는 자리는 스크립트로 문자열을 치환하는 대목이다. BOM 세 바이트가 첫 줄 맨 앞에 있어서 `^<Project` 처럼 줄 앞을 잡는 패턴이 아무 말 없이 안 맞는다. 편집기로 고칠 때는 이 문제가 없다.`
  ("아무 말 없이 안 맞는다" 는 용어집 `BOM` 행의 표현이므로 그대로 둔다.)

- [병합 4.1 17-B] 더할 불릿의 "지금 SDK" 를 노트가 쓰는 표기로 맞춰라. 노트는 "SDK 10.0.111"
  또는 "SDK 10" 만 쓴다.
  병합안: `- 경로에서 솔루션 폴더를 만드는 것은 지금 SDK 에서 새로 생긴 동작이 아니다. …`
  수정형: `- 경로에서 솔루션 폴더를 만드는 것은 SDK 10 에서 새로 생긴 동작이 아니다. `dotnet sln add` 의 `--in-root` 가 이 동작을 끄는 옵션이고 기본값은 끄지 않는 쪽이다. `-f sln` 으로 만든 `.sln` 에도 같은 폴더가 SolutionFolder 항목과 NestedProjects 절로 들어간다. SDK 10 에서 달라진 것은 그 폴더를 적는 표기뿐이다.`
  (자리는 위 "구조" 절의 지시대로 91행 다음이다.)

- [병합 4.1 17-C] "만드는 것까지다" 가 어색하다.
  병합안: `- SDK 10 에서 달라진 것은 `dotnet new sln` 이 `.slnx` 를 만드는 것까지다. …`
  수정형: `- SDK 10 에서 달라진 것은 `dotnet new sln` 의 산출물이 `.slnx` 라는 점이다. `dotnet sln add` 가 경로에서 솔루션 폴더를 만드는 것은 예전 `.sln` 에서도 같고 적는 표기만 다르다. 이 폴더 항목은 분류일 뿐이고 F# 컴파일 순서와 관계가 없다.`

- [병합 4.2 / 전문가 17:200] 표 칸의 "원서가 인쇄한 출력 표기" 와 "SDK 6.0.x 기준이면" 이
  줄어든 말이라 칸 안에서 읽히지 않는다.
  전문가안: `| 대상 프레임워크 | `net5.0`(원서가 인쇄한 출력 표기. SDK 6.0.x 기준이면 `net6.0`) | `net10.0` |`
  수정형: `| 대상 프레임워크 | `net5.0`(원서가 인쇄한 유일한 값. 원서가 쓴 SDK 6.0.x 를 따르면 `net6.0`) | `net10.0` |`
  (04챕터 50행의 "`net5.0`/`net6.0`" 과 어긋나지 않는다.)

- [병합 6.2 17:44] "것 중 … 것이" 가 겹치고, 더할 불릿의 "이유는 아래에 적었다" 가 무엇의
  이유인지 흐리다.
  병합안: `- 원서 스크립트와 달라진 것 중 짚어 둘 것이 셋이다.` / `- 이 밖에 `mkdir` 두 줄과 `dotnet sln add` 두 줄을 각각 한 줄로 묶었다(이유는 아래에 적었다).`
  수정형: `- 원서 스크립트와 달라진 점 가운데 짚어 둘 것이 셋이다.` / `- 이 밖에 `mkdir` 두 줄과 `dotnet sln add` 두 줄을 각각 한 줄로 묶었다(`dotnet sln add` 를 한 번만 불러도 되는 이유는 아래 절에 적었다).`

- [병합 6.2 17:211 / 전문가 17:211] 교체문 첫 문장을 3행의 표현과 맞춰라. 3행이 이미
  "이 부록은 개념을 설명하는 곳이 아니라 명령을 순서대로 늘어놓은 곳이다" 로 열고 있다.
  병합안: `- 이 부록은 개념을 설명하지 않는다. 산출물을 짚는 데 필요한 만큼만 규칙을 언급했다. …`
  수정형: `- 이 부록은 개념을 설명하는 곳이 아니다. 산출물을 짚는 데 필요한 만큼만 규칙을 언급했다. 솔루션과 프로젝트의 경계, `.fsproj` 의 컴파일 순서, 네임스페이스와 모듈, 테스트 작성은 4챕터 노트로 간다.`

- [병합 6.2 17:167 / 전문가 17:167] "파일 모양만 보인다" 의 주어가 없다. 138행이 같은 사정을
  "선언 없이 적었다" 로 쓰고 있으므로 그 어투에 맞춘다.
  전문가안: `이 블록은 `namespace` 로 시작하므로 `.fsx` 로는 돌릴 수 없다(오류 FS0010). 검증 대상에서 빼고 파일 모양만 보인다.`
  수정형: `이 블록은 `namespace` 로 시작하므로 `.fsx` 로는 돌릴 수 없다(오류 FS0010). 그래서 실행 단위에서 빼고 파일 모양만 적었다.`

- [병합 5.3 17-I] `- 이 부록의 알맹이는 여덟 단계다.` 는 그대로 적용해도 된다. 뒤 열거가
  여덟 항목이어서 계수가 맞는다. 확인만 해 두었다.

---

## 04챕터와 어긋나는 자리 (경고, 04챕터 집필자에게 함께 전달할 것)

두 노트가 같은 명령·같은 패키지를 다루므로 한쪽만 고치면 어긋나는 자리다.

- 04챕터 42-46행(`cd src/Shop` 뒤 `dotnet run`)은 이번 수정 대상이 아니다. 그대로 두어라.
  전문가 17:130 교체문이 "4챕터 노트가 쓴 `cd src/Shop` 뒤 `dotnet run` 과 결과가 같다" 로
  04챕터의 형태를 직접 인용하므로, 04챕터를 `dotnet run --project` 로 바꾸면 그 문장이 즉시
  낡는다. 반대로 04챕터를 그대로 두면 두 노트가 맞물린다.

- 04챕터 35행(`FsUnit` 대 `FsUnit.xUnit`)과 34행(참조 방향·MSB4006)도 손대지 마라.
  17챕터 44·113·114행이 "이유는 4챕터 노트에 적었다" 로 그리로 넘긴다.

- 04챕터 72행("순서를 뒤집으면 … 아직 존재하지 않는 이름을 참조하는 셈이 되어 컴파일에
  실패한다")은 고칠 필요가 없다. 04챕터 예제는 `Program.fs` 가 `Learners.fs` 를 실제로 쓰는
  경우이므로 사용처 오류가 맞고, 17-G 가 넣는 FS0222 는 부르지 않는 경우다. 두 서술이 서로
  어긋나지 않으며, 17-G 의 "이 규칙은 4챕터 노트에 있다" 는 04챕터 117-124행을 가리켜 성립한다.
  04챕터 72행을 FS0222 로 고치면 오히려 04챕터 안에서 117-124행과 겹친다.

- 04-A 적용 뒤 04챕터 50행이 "차이는 두 군데다" 로 바뀌고 솔루션 폴더가 별도 불릿이 되면,
  17챕터 쪽 문면(17-A 표 행 + 17-B 새 불릿 + 17-C 정리)과 사실이 완전히 일치한다. 대상
  프레임워크도 04챕터 50행의 "`net5.0`/`net6.0`" 과 17챕터 표 칸이 같은 값을 가리킨다.
  두 노트 사이에 남는 어긋남은 없다.

- 04-A 새 불릿의 "이 폴더는 편집기 솔루션 탐색기의 분류일 뿐이고 디스크의 디렉터리도 컴파일
  순서도 아니다" 는 17챕터 91행과 같은 말이다. 두 노트가 따로 읽히므로 중복은 문제가 아니다.
  다만 04챕터에서도 디스크 쪽은 "디렉터리" 로 적어야 용어집 `solution folder` 행의 갈라 쓰기가
  지켜진다(04-A 교체문은 이미 그렇게 되어 있다).

- 04-B 로 04챕터 36행에 "CLI 메시지" 와 "셸 로케일" 이 처음 들어간다. 용어집 `CLI` 행의 용례가
  "17챕터", `locale` 행의 용례가 "1·17챕터" 로 적혀 있어 두 행의 용례 칸이 낡는다. 용어집은
  F# 전문가만 편집하므로 여기서는 보고만 한다 — 두 행의 용례를 "4·17챕터", "1·4·17챕터" 로
  넓히는 것이 맞다. 표기 자체는 04-B 교체문이 용어집과 일치하므로 노트 쪽에 고칠 것은 없다.

- 04-B 교체문은 문장 순서가 17챕터 47행과 반대다(04 는 인용 안내 → 로케일 설명, 17 은
  로케일 설명 → 인용 안내). 사실은 같고 각 노트의 문맥에 맞으므로 그대로 두어도 된다.
  통일하고 싶다면 17챕터 47행이 아니라 04챕터 쪽을 17챕터 어순에 맞춰라 — 17챕터 47행은
  바로 아래 인용 블록을 가리키는 자리라서 현재 순서가 자연스럽다.
