# 00챕터 최종 검수 지시서 (용어·문법·문체·구조)

대상: `docs/ko/00-preface-getting-started.md` (159줄)
기준: `docs/ko/GLOSSARY.md` (126항목), `docs/ko/_pipeline/STYLE.md`
선행: `.cache/review/00-expert.md` (기술 검수), `.cache/review/batchA-glossary-merge.md`

전제. 아래 지시는 전문가 지시서의 교체안이 **모두 적용된 뒤**의 문서를 가정한다.
줄번호는 현재(수정 전) 파일 기준이다. 전문가안 자체를 고쳐야 하는 항목은 마지막 절에 모았다.

기계 검사 결과 (참고)
- 금지 표기 grep(`표현식|부작용|일급 값|컴파일 에러|씨드|가진다|가져야|가지므로|N장|**|](examples/|docs/ko/examples`) → 빈 결과. 통과.
- 실행 단위 `id` → `00-fsi-smoke-test` 3블록뿐. 대조 표(157줄)와 일치. 통과.
- 절 제목 7개 모두 `(원서 p.NN)` 표기가 있고 대조 표와 일치. 통과.

---

## 용어 일관성

- [00:14] `FSI` — 이 챕터에서 `FSI` 가 처음 나오는 자리인데 병기가 없다. 병기는 81줄 절 제목뿐이고, 절 제목은 원서 영문 제목을 그대로 인용한 것이라 병기로 셀 수 없다. 용어집 `F# Interactive (FSI)` 항목은 "첫 등장만 F# Interactive(FSI) 로 병기"를 요구한다.
  현재: "... 컬렉션은 5챕터, REPL 은 이 챕터의 FSI 절이 각각 담당한다."
  수정: "... 컬렉션은 5챕터, REPL 은 이 챕터의 F# Interactive(FSI) 절이 각각 담당한다."
  (81줄 제목과 157줄 대조 표의 `F# Interactive (FSI)` 는 원서 절 제목이므로 괄호 앞 공백까지 그대로 둔다. 84줄의 `F# Interactive 창` 은 VS Code 창 이름이므로 그대로 둔다.)

- [00:115] `기본 타입 추론` → `타입 추론` (용어집 `primitive` 비고: "기본 타입" 쓰지 않음. 병합 보고서 4.4 권고. 여기서 "기본"은 `primitive` 뜻이 아니지만, 같은 배치에서 01챕터 29줄의 "기본 타입"을 "원시 타입"으로 고치므로 문자열이 남아 있으면 검색·대조에서 오해를 부른다.)
  현재: "타입 주석(type annotation)을 하나도 적지 않았는데 `int` 로 정해진 것은 `+` 의 기본 타입 추론(type inference) 결과다."
  수정: "타입 주석(type annotation)을 하나도 적지 않았는데 `int` 로 정해진 것은 `+` 의 타입 추론(type inference) 결과다."

- [00:115] 같은 문장 안에서 `int` 뒤 조사가 갈린다. 앞은 자음 종성 취급(`이 나온다`), 뒤는 모음 종성 취급(`로 정해진`).
  현재: "... `val add: a: int -> b: int -> int` 이 나온다."
  수정: "... `val add: a: int -> b: int -> int` 가 나온다."
  (읽는 소리 "인트"를 기준으로 `가`/`로` 로 맞춘다. 뒤쪽 `int` 로 정해진 은 그대로 둔다.)

- [00:79] 조사 오류. 68줄이 같은 토큰을 자음 종성으로 취급해 `을` 을 쓴다(`-lang "F#"` 을 빼면).
  현재: "`dotnet new xunit -lang "F#"` 로 만든다."
  수정: "`dotnet new xunit -lang "F#"` 으로 만든다."

- [00:18] 버전 토큰 뒤 공백이 이 노트의 관례(`F# 10 이다`, `.NET 10 에서도`, `SDK 10.0.111 이`)와 어긋난다.
  현재: "F# 5와 6에서 들어온 기능을 반영했다."
  수정: "F# 5 와 6 에서 들어온 기능을 반영했다."

- 판단해 그대로 두는 것 (지적 아님, 집필자 혼동 방지용)
  - `폴더`(48줄, 66줄)와 `디렉터리`(전문가 8번 주석의 "현재 디렉터리")는 가리키는 대상이 다르다. 앞은 원서가 시키는 챕터별 작업 폴더, 뒤는 셸의 현재 디렉터리라는 표준 용어다. 통일하지 않는다.
  - 표 셀(24줄 `커링`, 27줄 `컬렉션`, 31줄 `원시 타입`)에는 병기를 넣지 않는다. 챕터 주제를 늘어놓는 자리이고, 커링·계산 식은 본문에서 병기가 붙는다.

- 파이프라인 담당자에게 넘길 챕터 간 불일치 (이 파일은 손대지 않음)
  - `int` 뒤 조사: 00챕터는 위 지시대로 `가`/`로`, `03-null-and-exceptions.md:204` 는 `으로`. 다음 배치에서 하나로 정해야 한다.
  - 원서 판본 표기: 00챕터 3줄 `2023년 1월 판`, `04-organising-code-and-testing.md:50` `2023년 1월판`.

---

## 문법·문체

- [00:10] 원서 목록에서 AI 누락 (전문가 15번). 병기·용어 문제 없음. 교체 문장을 확정해 둔다.
  현재: "웹, 클라우드, 머신러닝, 데이터 과학 같은 목록을 늘어놓기보다 그냥 프로그래밍에 좋다는 쪽이 더 정확하다는 것이다."
  수정: "웹, 클라우드, 머신러닝, AI, 데이터 과학 같은 목록을 늘어놓기보다 그냥 프로그래밍에 좋다는 쪽이 더 정확하다는 것이다."

- [00:21-37] 챕터 구성 표의 셀 종결이 섞여 있다. 평서형 `-다` 로 끝나는 셀 9개와 명사로 끝나는 셀 6개가 번갈아 나온다. STYLE.md 문체 규칙(평서형 종결)에 맞춰 평서형으로 통일한다. 전문가 5·13·14번 교체안도 이 통일에 맞춰 손봤다(11·14번은 명사 종결이었다).
  | 2 | Functions | 함수의 규칙, 함수 합성, 커링, 부분 적용을 다루고 시그니처를 읽는 법을 익힌다 |
  | 4 | Organising Code and Testing | 솔루션과 프로젝트, 네임스페이스와 모듈, 파일 순서를 정리하고 xUnit 과 FsUnit 으로 테스트를 처음 써 본다 |
  | 5 | Introduction to Collections | 컬렉션 함수를 파이프라인으로 이어 데이터를 변환한다. `Seq` 와 `Array` 는 종류만 소개하고 실습은 `List` 로 한다 |
  | 8 | Functional Validation | 6챕터에서 읽은 데이터에 검증을 붙이고, 계산 식(computation expression)을 처음 다룬다 |
  | 10 | Object Programming | 함수 우선 언어인 F# 이 지원하는 객체 프로그래밍 기능을 살펴본다 |
  | 11 | Recursion | 누적기를 쓴 꼬리 재귀, `List.fold` 로 다시 쓰기, 퀵소트와 트리 순회를 다룬다 |
  | 12 | Computation Expressions | `Option`, `Result`, `Async` 같은 효과를 다루는 일반적 장치로서 계산 식을 파고든다 |
  나머지 8개 행(1, 3, 6, 7, 9, 13, 14, 15)은 이미 평서형이므로 건드리지 않는다. 6·15행의 ". 짧은 챕터" 꼬리표는 두 행이 같은 형태이므로 유지한다.
  8행 수정 이유를 덧붙인다. 현재 "검증을 붙이고 ... 처음 만난다"는 앞 절의 주체(챕터)와 뒷 절의 주체(독자)가 어긋난다. "처음 다룬다"로 주체를 챕터에 맞춘다.

- [00:44] "25년 경력" → 원서는 "over 25 years"(전문가 16번). "경력을 가진"으로 쓰면 STYLE 금지 패턴 1(have 직역)에 걸리므로 아래 형태로 적는다.
  현재: "저자 소개(Ian Russell, 영국의 25년 경력 개발자, Trustbit 근무)와 감사의 말..."
  수정: "저자 소개(Ian Russell, 25년 넘게 일한 영국 개발자, Trustbit 근무)와 감사의 말..."

- [00:50] 하위 호환 서술 범위 좁히기(전문가 9번). 전문가가 문장을 제시하지 않았으므로 확정형을 준다.
  현재: "원서 코드는 F# 5~6 기준으로 쓰였지만 그 사이 F# 은 하위 호환을 지켜 왔으므로 원서 예제는 .NET 10 에서도 그대로 돌아간다."
  수정: "원서 코드는 F# 5~6 기준으로 쓰였지만 그 사이 F# 은 하위 호환을 지켜 왔으므로 원서의 F# 문법과 코어 라이브러리 예제는 .NET 10 에서도 그대로 돌아간다. 13~15챕터의 웹 실습만 Giraffe 패키지 버전과 ASP.NET Core 호스팅 API 가 달라져 있어 그 챕터에서 버전을 맞춰 적는다."

- [00:144] 위 수정의 파급. 정리 절에 옛 서술만 남는다. 전문가가 지적하지 않은 자리다.
  현재: "- 원서의 .NET 6 기준 서술은 2026년 현재 .NET 10 으로 읽으면 된다. 하위 호환이 지켜져 원서 예제는 그대로 동작한다."
  수정: "- 원서의 .NET 6 기준 서술은 2026년 현재 .NET 10 으로 읽으면 된다. F# 문법과 코어 라이브러리 예제는 하위 호환이 지켜져 그대로 동작하고, 13~15챕터의 웹 실습만 패키지 버전을 맞춰야 한다."

- [00:66] 검증 스크립트 추출 위치(전문가 3번). 전문가안의 "임시 디렉터리" 대신 파이프라인 담당자가 확정한 표현을 쓴다. `KEEP=1` 안내는 스크립트 7줄·63줄과 일치하므로 그대로 살린다.
  현재: "- 폴더 구성은 원서 방식대로 책 폴더 하나를 만들고 그 안에 챕터별 폴더를 두는 것으로 충분하다. 이 저장소는 노트가 `docs/ko/` 에 있고 실습 코드는 검증 스크립트가 `.cache/examples/` 로 뽑아 실행한다."
  수정: "- 폴더 구성은 원서 방식대로 책 폴더 하나를 만들고 그 안에 챕터별 폴더를 두는 것으로 충분하다. 이 저장소는 노트가 `docs/ko/` 에 있고, 실습 코드는 검증 스크립트가 시스템 임시 경로로 뽑아 실행한다. 뽑아낸 스크립트를 직접 보고 싶으면 `KEEP=1` 을 붙여 실행하면 경로를 알려 준다."

- [00:85] 피동 연쇄(STYLE 금지 패턴 5).
  현재: "- 코드를 골라 `ALT+ENTER` 를 누르면 그 부분이 FSI 로 복사되어 컴파일되고 실행된다. ..."
  수정: "- 코드를 골라 `ALT+ENTER` 를 누르면 그 부분이 FSI 로 넘어가 컴파일되고 실행된다. ..."

- [00:87] `id` 한정(전문가 10번). 교체 문장을 확정해 둔다.
  현재: "... 이 저장소의 검증 스크립트는 `dotnet fsi <파일>.fsx` 를 쓴다. 노트의 코드 블록이 전부 이 명령으로 실행되므로 알아 두는 편이 낫다."
  수정: "... 이 저장소의 검증 스크립트는 `dotnet fsi <파일>.fsx` 를 쓴다. `id` 가 붙은 코드 블록은 전부 이 명령으로 실행되므로 알아 두는 편이 낫다."

- [00:97] 스모크 테스트 도입 문장. 전문가 1번(스크립트 실행에서는 `val` 줄이 안 나온다)과 7번(`FSI: Send File`)이 같은 문장을 건드리므로 하나로 합친 확정형을 준다. `dotnet fsi` 뒤에 파일 이름을 붙여 89-95줄 bash 블록과 형태를 맞춘다.
  현재: "아래는 설치 직후 환경이 제대로 동작하는지 확인하는 최소 스크립트다. 파일로 저장해 `dotnet fsi` 로 돌리거나, VS Code 에서 전체를 골라 `ALT+ENTER` 로 FSI 에 보내면 된다."
  수정: "아래는 설치 직후 환경이 제대로 동작하는지 확인하는 최소 스크립트다. 파일로 저장해 `dotnet fsi smoke.fsx` 로 돌리거나, VS Code 에서 전체를 골라 `ALT+ENTER` 로 FSI 에 보내거나, 명령 팔레트에서 `FSI: Send File` 을 쓰면 된다."

- [00:100] 실행 단위 머리 주석. 문장 구조가 어긋나 있고("값 바인딩, 함수 바인딩, printfn 이 동작하는지 확인" — 앞 두 항목은 개념 나열, 뒤 항목은 확인 행위), 전문가 2번·12번이 블록을 늘리면 주석이 단위 내용과도 어긋난다.
  현재: "// 이 단위가 보여주는 것: 값 바인딩, 함수 바인딩, printfn 이 동작하는지 확인"
  수정: "// 이 단위가 보여주는 것: 값 바인딩, 함수 바인딩, FSI 의 정의 유지, 런타임 버전 확인"

- [00:123] 블록 개수 서술. 전문가 12번이 블록을 하나 더하면 "세 블록"이 거짓이 된다. 전문가가 지적하지 않은 자리다. 개수에 의존하지 않는 표현으로 바꾼다.
  현재: "이 세 블록이 오류 없이 돌아가면 환경 설정은 끝났다. 세 블록은 같은 `id` 를 쓰므로 문서 순서대로 이어 붙어 하나의 스크립트로 실행된다."
  수정: "위 블록들이 오류 없이 돌아가면 환경 설정은 끝났다. 같은 `id` 를 쓴 블록은 문서 순서대로 이어 붙어 하나의 스크립트로 실행된다."

- [00:130] 전문가 4번 교체안이 127-130줄 불릿 넷을 통째로 바꾸면서 "이 노트의 코드 블록은 모두 `.fsx` 로 뽑혀 검증된다"가 사라진다. 전문가 10번은 이 문장을 고치라고 하는데 고칠 대상이 없어진다. 문장을 살릴 것이라면 전문가 4번 교체 불릿의 마지막 줄 뒤에 아래 한 문장으로 붙인다.
  추가: "이 노트에서 `id` 가 붙은 코드 블록은 모두 `.fsx` 로 뽑혀 검증된다."
  살리지 않기로 하면 전문가 10번의 130줄 지시는 폐기 처리하고 87줄만 고친다. 둘 중 하나를 고르되 두 곳 모두 손대지 않은 상태로 남기지 마라.

- [00:136] 문장이 명사구로 끝난다(STYLE 문체: 평서형 `-다` 종결).
  현재: "- 저자가 마지막에 붙인 당부는 하나다. 책에 코드가 많이 나오는데 복사해 붙이지 말고 직접 타이핑하라는 것. 손으로 쳐 보는 쪽이 더 많이 남는다는 경험담이다."
  수정: "- 저자가 마지막에 붙인 당부는 하나다. 책에 코드가 많이 나오는데 복사해 붙이지 말고 직접 타이핑하라는 것이다. 손으로 쳐 보는 쪽이 더 많이 남는다는 경험담이다."

- [00:147] 전문가 4번의 파급. `.fsx`/`.fs` 의 차이를 "컴파일 대상 여부"에서 "프로젝트 파일과 지시문"으로 다시 잡으면, 정리 절에 옛 프레임만 남는다. 전문가가 지적하지 않은 자리다.
  현재: "- `.fsx` 는 시험용, `.fs` 는 빌드 대상이다. FSI 전송은 둘 다 된다."
  수정: "- `.fsx` 는 프로젝트 파일 없이 혼자 돌고 `#r`·`#load` 지시문을 쓸 수 있다. `.fs` 는 프로젝트에 담아 빌드한다. FSI 전송은 둘 다 된다."

---

## 구조

- 대조 표(151-159줄)는 손대지 않는다. 전문가 4번이 더하는 `id` 없는 블록은 실행 단위가 아니므로 `Script Files vs Code Files` 행의 `—` 를 그대로 둔다. 새 행을 만들지 마라.
- 전문가 12번 블록의 배치. "두 번째 블록 뒤"라고만 적혀 있는데, 그 자리에 넣으면 `add` 시그니처를 설명하는 115-116줄 불릿과 코드 사이가 끊긴다. 115-116줄 불릿 뒤, 런타임 확인 블록 앞에 넣고 앞뒤에 한 줄씩 설명을 둔다(STYLE: 블록이 설명 사이사이에 오게 한다. 블록 두 개를 붙여 놓지 않는다).
  블록 앞에 넣을 문장: "FSI 가 정의를 세션에 남겨 둔다는 것은 이렇게 확인한다. 앞 블록에서 만든 두 정의가 그대로 살아 있다."
  블록 뒤(런타임 확인 블록 앞)에 넣을 문장: "마지막으로 실행 중인 런타임을 찍어 설치 상태를 확인한다."
- 전문가 4번이 더하는 `id` 없는 `fsharp` 블록도 앞뒤에 설명이 필요하다. 불릿 목록 끝에 블록만 덜컥 붙이지 말고 아래 한 줄을 앞에 둔다.
  "지시문의 모양만 미리 보면 이렇다. 13챕터부터 Giraffe 패키지를 끌어올 때 이 두 줄을 쓴다."
- `printfn` 열 정렬 확인 결과.
  - 현재 104줄과 112줄의 `//` 는 둘 다 42열에서 시작한다. 전문가 12번이 제안한 `printfn "%s (%d)" greeting (add 40 2)    // 기대: ...` 도 42열이다. 그대로 붙여넣으면 정렬이 맞는다. 공백 개수를 임의로 줄이지 마라.
  - 전문가 2번이 바꾸는 런타임 블록은 `printfn` 줄 자체가 90자를 넘으므로 기대 출력 주석을 다음 줄에 둔 형태가 맞다. 이 블록에서 트레일링 주석을 42열에 억지로 맞추려 하지 마라. 나머지 두 블록의 42열 정렬은 이 변경에 영향받지 않는다.
- 코드 블록 배치, 절 구성, `(원서 p.NN)` 표기, `](examples/` 나 `docs/ko/examples` 잔존 — 모두 문제 없다.

---

## 선행 검수 지시서의 문제

- [14번] 4챕터 행 교체안에 `XUnit` 이 들어 있다. 용어집 `xUnit` 항목 위반이다(원서 "XUnit" 표기를 따르지 않는다). 위 "문법·문체" 절의 표 통일안이 이미 반영한 형태를 쓴다.
  전문가안: "| 4 | Organising Code and Testing | 솔루션과 프로젝트, 네임스페이스와 모듈, 파일 순서, XUnit 과 FsUnit 으로 테스트 첫 경험 |"
  수정형: "| 4 | Organising Code and Testing | 솔루션과 프로젝트, 네임스페이스와 모듈, 파일 순서를 정리하고 xUnit 과 FsUnit 으로 테스트를 처음 써 본다 |"

- [13번] 11챕터 행 교체안이 명사 종결이라 표의 통일을 깬다.
  전문가안: "| 11 | Recursion | 누적기를 쓴 꼬리 재귀, `List.fold` 로 다시 쓰기, 퀵소트와 트리 순회 |"
  수정형: "| 11 | Recursion | 누적기를 쓴 꼬리 재귀, `List.fold` 로 다시 쓰기, 퀵소트와 트리 순회를 다룬다 |"

- [5번] 5챕터 행 교체안은 용어·문체 문제 없다. 그대로 쓴다.

- [4번] 교체 불릿 셋에 손볼 데가 세 곳 있다.
  1. `지시문`(용어집 `compiler directive`)이 이 챕터에서 처음 나오는 자리인데 병기가 없다. 그리고 앞 불릿이 `#r` 과 `#load` 둘을 들었으므로 "이 지시문은"은 수가 맞지 않는다.
     전문가안: "- 이 지시문은 컴파일러가 확장자를 보고 막는다. `.fs` 에 `#r` 을 쓰면 `error FS0076` 이 나고, `dotnet fsi` 도 `.fs` 파일은 스크립트로 실행하지 않는다."
     수정형: "- 이 두 지시문(compiler directive)은 컴파일러가 확장자를 보고 막는다. `.fs` 에 `#r` 을 쓰면 `error FS0076` 이 나고, `dotnet fsi` 도 `.fs` 파일은 스크립트로 실행하지 않는다."
  2. "필요한지다"가 어색하고, 같은 문장에 "파일"이 세 번 나온다.
     전문가안: "- 가장 큰 차이는 프로젝트 파일이 필요한지다. `.fsx` 는 혼자 실행된다. `dotnet fsi <파일>.fsx` 로 바로 돌아가고, 필요한 패키지와 다른 파일을 파일 안에서 `#r "nuget: ..."` 와 `#load "다른.fsx"` 로 직접 끌어온다. `.fs` 는 의존성을 `.fsproj` 에서 받는다."
     수정형: "- 가장 큰 차이는 프로젝트 파일이 필요한지에 있다. `.fsx` 는 혼자 실행된다. `dotnet fsi <파일>.fsx` 로 바로 돌아가고, 필요한 패키지와 다른 스크립트를 `#r "nuget: ..."` 와 `#load "다른.fsx"` 로 자기 안에서 직접 끌어온다. `.fs` 는 의존성을 `.fsproj` 에서 받는다."
  3. 마지막 불릿에 위 "문법·문체" 절 [00:130] 항목의 문장을 붙일지 여기서 결정한다.
  나머지 불릿(파일 두 종류, 빌드 관행, `ALT+ENTER`, 실무 감각)은 용어·문체 문제 없다.

- [1번] 교체 불릿의 마지막 문장이 사역 표현으로 뭉개져 있고, 바로 뒤에 붙이는 bash 명령과 연결되지 않는다.
  전문가안: "스크립트 파일에서 시그니처를 확인하려면 표준 입력으로 밀어 넣어 세션으로 처리하게 한다."
  수정형: "스크립트 파일의 시그니처를 보려면 `dotnet fsi --nologo < smoke.fsx` 처럼 표준 입력으로 밀어 넣어 대화형 세션으로 실행한다."
  같은 항목의 bash 주석은 89-95줄 다른 주석들(`# 스크립트 파일을 명령줄에서 실행`, `# 대화형 세션으로 진입 (입력 끝에 ;; 필요, #quit;; 으로 종료)`)이 모두 명사구 + 괄호 보충 형태인데 혼자 완결 문장이다.
  전문가안: "# 스크립트를 표준 입력으로 밀어 넣으면 정의마다 val 줄이 보인다"
  수정형: "# 표준 입력으로 밀어 넣어 실행 (정의마다 val 줄이 보인다)"
  첫 문장과 두 번째 문장("VS Code 에서 `ALT+ENTER` 로 보낼 때, 그리고 `dotnet fsi` 로 진입한 세션에 직접 입력할 때가 그렇다")은 문제 없다. `--nologo` 는 백틱 안의 플래그이므로 병기 대상이 아니다.

- [2번] 기대 출력 주석의 형식이 이 문서의 다른 두 자리와 어긋난다. 런타임 값은 실행 환경마다 달라지므로 현재 120줄이 쓰는 "이 저장소 출력 예" 를 유지해야 독자가 자기 값과 다른 것을 오류로 읽지 않는다. 괄호 앞 두 칸 공백도 한 칸으로 줄인다.
  전문가안: "// 이 저장소 출력: 런타임 = .NET 10.0.11  (SDK 10.0.111 과 앞의 10.0 이 같다)"
  수정형: "// 이 저장소 출력 예: 런타임 = .NET 10.0.11 (SDK 10.0.111 과 앞의 10.0 이 같다)"
  첫 줄 주석("// 실행 중인 런타임. SDK 와 메이저·마이너 버전이 같은지 눈으로 확인한다")은 문제 없다. `런타임`(용어집 `runtime`)은 50줄 등에서 이미 쓰인 표기이고 병기는 필요 없다.

- [3번] 교체 불릿의 "임시 디렉터리로 뽑아 실행한다" 는 위 [00:66] 항목의 "시스템 임시 경로로 뽑아 실행한다" 로 대체한다(파이프라인 담당자 확정). 같은 불릿 안에서 `폴더` 와 `디렉터리` 가 섞이는 것도 함께 없어진다.

- [6번] 부록 안내 교체 불릿의 두 번째 문장이 네 절짜리 한 문장이라 읽기 어렵다. 두 문장으로 끊는다.
  전문가안: "- 4챕터는 솔루션 하나에 코드 프로젝트와 테스트 프로젝트를 나눠 담는 구성에서 출발하는데, 그 절차는 원서 본문이 아니라 부록 1(원서 pp.195-196)에 있다. `dotnet new sln` 으로 솔루션을 만들고 `dotnet sln add` 로 두 프로젝트를 넣은 뒤, 테스트 프로젝트에서 `dotnet add reference` 로 코드 프로젝트를 참조하고 `dotnet add package FsUnit.xUnit` 을 더한다."
  수정형: "- 4챕터는 솔루션 하나에 코드 프로젝트와 테스트 프로젝트를 나눠 담는 구성에서 출발한다. 그 절차는 원서 본문이 아니라 부록 1(원서 pp.195-196)에 있다. `dotnet new sln` 으로 솔루션을 만들고 `dotnet sln add` 로 두 프로젝트를 넣는다. 그다음 테스트 프로젝트에서 `dotnet add reference` 로 코드 프로젝트를 참조하고 `dotnet add package FsUnit.xUnit` 을 더한다."
  같은 항목이 16-19줄에 부록 존재를 한 줄 적으라고 권하는데 문장이 없다. 19줄 뒤에 아래 불릿으로 넣는다.
  추가형: "- 원서는 15챕터 뒤에 부록 둘을 더 붙인다. 부록 1 은 VS Code 에서 솔루션과 프로젝트를 만드는 절차(원서 pp.195-196), 부록 2 는 15챕터에서 쓰는 CSS 와 JavaScript 코드다."
  첫 불릿("4챕터의 테스트 실습에 필요한 템플릿도 ...")에는 위 [00:79] 조사 수정(`로` → `으로`)을 함께 적용한다.

- [7번] Ionide 확장 교체 불릿의 세 번째 문장이 서술어 없이 끝난다.
  전문가안: "명령줄로는 `code --install-extension Ionide.Ionide-fsharp` 다."
  수정형: "명령줄에서는 `code --install-extension Ionide.Ionide-fsharp` 로 설치한다."
  같은 항목이 97줄에 제안한 다듬기는 위 [00:97] 항목의 확정형으로 대체한다(전문가 1번과 겹치는 문장이라 하나로 합쳤다). `Ionide` 표기는 용어집과 일치하고, 확장 식별자는 백틱 안이므로 병기 대상이 아니다.

- [8번] `dotnet --version` 주석 교체안은 명사구 형태라 다른 bash 주석과 맞고 용어 문제도 없다. 그대로 쓴다.

- [9번] 문장이 제시되지 않았다. 위 [00:50] 확정형을 쓰고, [00:144] 정리 불릿까지 함께 고친다.

- [10번] 위 [00:87] 확정형을 쓴다. 130줄 지시는 [00:130] 항목의 판단에 따른다.

- [11번] 교체 문장("저자는 F# 개발에서 중단점을 걸고 디버깅한 일이 한 번도 없다고 적는다")은 용어집 `breakpoint`=중단점과 일치하고 평서형이며 문제 없다. 그대로 쓴다. 다만 83줄에서 이 문장만 갈아 끼우고 뒷문장("코드가 맞는지 확인하는 수단이 디버거가 아니라 FSI 라는 것이다")은 유지한다.

- [12번] 블록 자체는 문제 없다. 배치와 앞뒤 설명, 그리고 이 블록 때문에 거짓이 되는 100줄·123줄 서술은 위 "구조"와 "문법·문체" 절 지시를 따른다.

- [15번] 위 [00:10] 확정형을 쓴다.

- [16번] 위 [00:44] 확정형을 쓴다("가진"을 피한 형태).

- [17번] 표 아래 각주 한 줄은 넣어도 좋다. 문체상 문제는 없으나 문장을 확정해 둔다. 표 바로 아래(38줄 자리)에 둔다.
  추가형: "표의 원서 제목은 서문 Contents 절 표기를 따랐다. 9챕터는 본문 제목이 단수형 Single-Case Discriminated Union 이고, 13~15챕터는 본문 제목에서 with 가 소문자다."

- 원문 의존도. 직역투나 원서 문장 구조가 그대로 드러난 자리는 없다. `~에 대해`, `~을 통해`, `되어진` 은 검색 결과 0건이고, 26줄의 `F# 에서의` 하나가 일본어투였는데 표 통일안이 걷어낸다. 전문가 교체안에도 원서 문장을 옮긴 대목은 없다.
