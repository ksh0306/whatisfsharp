# 16챕터 최종 검수 지시서 (용어 일관성·한국어 문법·문체)

검수 대상: `docs/ko/16-closing-summary.md` (95줄, 실행 단위 0개)
기준: `docs/ko/GLOSSARY.md`(389줄, 데이터 행 355) / `docs/ko/_pipeline/STYLE.md`
선행: `16-expert.md`, `batchE-glossary-merge.md`(5.1·5.2·6.1)

게이트: `check-note.sh` OK, `verify-examples.sh` SKIP(코드 없음 — 정상).
이 지시서의 모든 교체문을 임시 파일에 모아 `check-note.sh` 로 돌려 금지 패턴 0건을 확인했다.
코드 블록이 하나도 없으므로 `printfn` 열 정렬에 영향을 주는 수정은 없다.
`](examples/...)`·`docs/ko/examples` 잔존 0건.

## 적용 순서 (중요)

병합 보고서(`batchE-glossary-merge.md`)의 5.1·5.2·6.1 을 먼저 적용하고, 그다음 이 지시서를 적용하라.
단 아래 항목에서 **이 지시서의 문면이 병합 교체문을 대체한다**(STYLE.md: 최종 검수 우선).

- 13줄(병합 16-6), 38줄(병합 16-1), 35줄(병합 16-4), 36줄(병합 16-5), 43줄(병합 16-9),
  48줄·5줄·8줄·14줄·73줄(병합 6.1), 75줄(병합 16-3), 76줄(병합 16-2), 85줄(병합 16-8).

각 항목에 "병합안 → 최종안" 을 함께 적었으니 그 문면 하나만 넣으면 된다.
줄 번호는 참고용이다. 찾을 문자열로 대상을 특정하고 `grep -c` 로 출현 1회를 확인한 뒤 치환하라.

---

## 용어 일관성

- [16:25] `` `.fsproj` 의 파일 순서 `` → `` `.fsproj` 의 컴파일 순서 ``
  근거: 용어집 `compile order`→컴파일 순서 의 정의가 "`.fsproj` 의 `<Compile Include=... />` 가
  적힌 순서" 다. 같은 노트 69줄이 같은 대상을 "4챕터의 컴파일 순서" 로 부르고 있어 한 문서 안에서
  두 표기가 갈린다. 13·14·15챕터도 전부 "컴파일 순서" 다(실측: 컴파일 순서 13곳, 파일 순서 4곳).
  찾을 문자열: `` | 4 코드 구성과 테스트 | `.fsproj` 의 파일 순서가 곧 의존 방향이라는 전제로 ``
  교체 행:
  ```
  | 4 코드 구성과 테스트 | `.fsproj` 의 컴파일 순서가 곧 의존 방향이라는 전제로 프로젝트를 나눈다. xUnit 과 FsUnit 으로 테스트를 짠다 |
  ```

- [16:52] `지은이 이름도` → `저자 이름도`
  근거: 바로 다음 행(53줄)이 같은 개념을 "제목과 저자를 확인했다" 로 적는다. 인접한 표 두 행이
  같은 것을 다른 낱말로 부른다. 표 안에서는 "저자" 가 원서 저자가 아니라 그 책의 저자로 읽히므로
  혼동도 없다.
  찾을 문자열: `지은이 이름도 링크에서 확인한 것이 아니다`
  교체: `저자 이름도 링크에서 확인한 것이 아니다`

- [16:59, 16:84] `봇 차단 화면`·`봇 차단` → `봇 확인 화면`
  근거: 같은 현상을 52줄은 "봇 확인 화면", 59줄은 "봇 차단 화면", 84줄 정리는 "봇 차단" 으로
  적어 한 문서에 세 표기가 있다. 정리(84줄)가 두 항목을 하나로 묶어 세므로 표의 두 행이 같은
  낱말이어야 셈이 읽힌다.
  찾을 문자열 1: `| 봇 차단 화면이 떠서 확인하지 못했다 |`
  교체 1: `| 봇 확인 화면이 떠서 확인하지 못했다 |`
  찾을 문자열 2: `둘은 봇 차단으로 확인하지 못했으며`
  교체 2: `둘은 봇 확인 화면 때문에 확인하지 못했으며`
  (두 화면이 실제로 다른 모습이었다면 표 두 행은 그대로 두고 84줄만
  `둘은 봇 확인·차단 화면 때문에 확인하지 못했으며` 로 적어라. 어느 쪽이든 세 표기가
  두 표기로 줄어야 한다.)

- [16:13 + 16:38] `F# Interactive(FSI)` 병기가 두 곳에 남는다 — 병합 교체문의 부작용
  병합 16-6 은 13줄에, 16-1 은 38줄에 각각 `F# Interactive(FSI)` 를 넣는다. 둘을 다 적용하면
  한 노트에 병기가 두 번 나오고(용어집: 첫 등장 1회), 게다가 13줄에서는 바로 앞 문장이 이미
  맨 FSI 를 쓰고 있어 병기가 첫 등장보다 뒤에 온다. 현재 노트에는 FSI 병기가 한 곳도 없고
  첫 등장은 13줄이다. 병기는 13줄 첫 등장 자리로 올리고, 절 이름은 두 곳 모두
  `F# Interactive 절` 로 적는다(서문 챕터 85줄의 절 제목이 `F# Interactive (FSI)` 이므로
  이 지칭이 어긋나지 않는다).
  → 아래 "구조" 절의 13줄·38줄 최종안을 쓰라.

- [16:76] 병합 16-2 교체문의 `같은 패키지의 taskResult` — 선행어가 없다
  이 노트는 `FsToolkit.ErrorHandling` 을 한 번도 언급하지 않는다. "같은 패키지" 가 가리킬 대상이
  문서 안에 없어 12챕터를 안 펼친 독자는 어느 패키지인지 알 수 없다. 용어집 `taskResult`·
  `asyncResult` 두 행 모두 패키지 이름을 명시한다.
  → 아래 "선행 검수 지시서의 문제" 의 76줄 최종안을 쓰라(패키지 이름을 괄호로 적었다).

- [16:76, 16:85] `핸들러는/핸들러가 손댈 곳이 없다` → `핸들러에는/핸들러에 손댈 곳이 없다`
  손대는 주체는 독자이고 핸들러는 손대는 대상이다. 병합 16-2 는 "핸들러는", 16-8 은 "핸들러가"
  로 적어 두 곳의 조사도 갈린다. 최종안에서 `에는`/`에` 로 맞췄다.

- [16:75] `15챕터 노트가 정리 절에서` — 그대로 둔다.
  `N챕터 노트` 는 노트 18개에서 10회 쓰인 형태이고(07·09·16챕터 각 1회, 17챕터 7회) 여기서는
  원서 15장이 아니라 15챕터 노트의 정리 절을 가리켜야 하므로 "노트" 가 뜻을 나른다. 위반이 아니다.

## 문법·문체

- [16:3] 현재: `원서의 자료 목록은 2023년 1월 기준이라 링크가 그대로인지 확인할 수 있는 것만 확인해 표시했다.`
  → 수정: `원서의 자료 목록은 2023년 1월 기준이라 링크가 지금도 그대로인지 하나씩 확인해 결과를 적었다.`
  ("확인할 수 있는 것만 확인해" 가 한 문장에 같은 낱말을 두 번 쓴다. 확인이 막힌 항목도 표에
  그대로 적혀 있으므로 "결과를 적었다" 가 사실에 맞다.)

- [16:3, 16:16] `손에 남은 것` / `손에 남는 것` 시제가 갈린다.
  16줄 소제목이 `### 열다섯 챕터에서 손에 남는 것` 이므로 3줄을 맞춘다.
  현재: `열다섯 챕터에서 손에 남은 것이 무엇인지 되짚는 표`
  → 수정: `열다섯 챕터에서 손에 남는 것이 무엇인지 되짚는 표`

- [16:8] 현재: `... 옮겨 놓은 자리였다.` → 수정: `... 옮겨 놓은 자리다.`
  (원서·다른 챕터를 가리키는 서술은 현재형으로 쓴다. 병합 16-9 가 43줄을 같은 이유로
  "나왔다"→"나온다" 로 고치므로 같은 규칙을 여기에도 적용한다.)
  찾을 문자열: `절이 그 감각을 그대로 실습으로 옮겨 놓은 자리였다.`

- [16:18] 현재: `서문 챕터의 표가 "각 챕터가 무엇을 다루는가"였으므로`
  → 수정: `서문 챕터의 표가 "각 챕터가 무엇을 다루는가"이므로`
  (그 표는 지금도 그 표다. 위 항목과 같은 시제 규칙.)

- [16:22] 현재: `| 1 도메인 모델링 | 명세를 읽으면서 AND 타입(튜플·레코드)과 OR 타입(판별 유니온)으로 옮긴다.`
  → 수정: `| 1 도메인 모델링 | 명세를 읽으면서 그 내용을 AND 타입(튜플·레코드)과 OR 타입(판별 유니온)으로 옮긴다.`
  ("명세를" 이 "읽으면서" 의 목적어로 이미 쓰여 "옮긴다" 의 목적어가 비었다.)

- [16:26] 현재: `` 기존 함수로 안 되어 `fold` 로 내려갈 때가 언제인지 안다 ``
  → 수정: `` 기존 함수로 풀리지 않아 `fold` 로 내려갈 때가 언제인지 안다 ``
  ("안 되어" 는 구어이고 무엇이 안 되는지가 비어 있다.)

- [16:42] 현재: `재단이 하는 언어와 커뮤니티 홍보 활동을 지원하는 뜻이다.`
  → 수정: `재단이 하는 언어와 커뮤니티 홍보 활동을 지원한다는 뜻이다.`
  ("지원하는 뜻" 은 비문이다. 관형형이 아니라 인용형 "-다는" 이 와야 한다.)

- [16:43] 현재: `가입하면 재단 Slack 에 들어갈 수 있고, 입문자용 채널이 따로 갖춰져 있다는 것이 저자가 드는 실질적인 이점이다.`
  → 수정: `가입하면 재단 Slack 에 들어갈 수 있다. 입문자용 채널이 따로 갖춰져 있다는 것이 저자가 드는 실질적인 이점이다.`
  (앞 절은 서술형 종결, 뒤 절은 "-는 것이 …이다" 명사 술어라 `-고` 로 이을 수 없다. 주술 불일치.)
  이 불릿의 마지막 문장은 병합 16-9 대로 `같은 권유가 서문 챕터에도 한 번 나온다(원서 pp.4-5).` 다.

- [16:44] 현재: `- 링크 확인: 재단 사이트 ...` → 수정: `- 링크는 이렇게 확인했다. 재단 사이트 ...`
  (노트 어디에도 `라벨:` 형태의 불릿 머리가 없다. 평서형으로 맞춘다.)
  같은 줄: `403 이 떨어져 확인하지 못했다` → `403 이 돌아와 확인하지 못했다` (구어 "떨어져").

- [16:58] 현재: `| 그 회사 사이트가 아니게 됐다. 아래 설명 참고 |`
  → 수정: `| 그 회사 사이트가 아니게 됐다. 자세한 것은 표 아래 설명에 있다 |`
  (표의 다른 일곱 칸은 모두 `-다` 로 끝나는 문장이다. 이 칸만 명사형 조각이다.)

- [16:61] 현재: `` 표에서 손이 가는 항목은 마지막 사이트다. `compositional-it.com` 은 지금 회사와 무관한 도메인으로 넘어간다. ``
  → 수정: `` 표에서 손이 가는 항목은 사이트 다섯 중 마지막인 Compositional-IT Blog 다. `compositional-it.com` 은 지금 그 회사와 무관한 도메인으로 넘어간다. ``
  (표의 마지막 행은 강의이므로 "마지막 사이트" 가 마지막 행으로 오독된다. 또 "지금 회사" 는
  "현재 직장" 으로 읽히므로 "그" 를 넣어야 Compositional IT 를 가리킨다.)

- [16:67] 현재: `- 마지막 절에서 저자가 하는 이야기는 F# 을 좋은 언어로 만드는 것이 개별 기능이 아니라 기능들이 서로 맞물리는 방식이라는 것이다. 근거로 아리스토텔레스의 널리 알려진 문장, 전체는 부분의 합보다 크다는 말을 든다.`
  → 수정: `- 마지막 절의 요지는 F# 을 좋은 언어로 만드는 것이 개별 기능만이 아니라 기능들이 서로 맞물리는 방식이라는 것이다. 근거로 전체는 부분의 합보다 크다는 아리스토텔레스의 널리 알려진 문장을 든다.`
  세 가지를 함께 고친 것이다. (1) `A, B라는 말을 든다` 꼴의 쉼표 동격은 영어 appositive 를
  그대로 옮긴 구조다. 관형절로 바꾸면 한 번에 읽힌다. (2) 7줄이 이미 "저자가 먼저 하는
  이야기는 …라는 것이다" 를 쓰므로 같은 틀이 두 번 나온다. (3) 8줄에 병합 6.1 이 넣는
  "만의/만은" 뉘앙스가 이 문장에도 그대로 걸린다(원문 "not just about the individual
  features"). 8줄만 고치면 두 문장의 강도가 어긋난다.
  같은 이유로 83줄의 `F# 의 값은 기능 목록이 아니라` 도 `F# 의 값은 기능 목록에만 있지 않고`
  로 맞추는 편이 낫다(정리 절이라 단정형을 남겨 두는 판단도 가능하다. 집필자가 정하라).

- [16:68] 현재: `- 같은 이야기가 서문에도 있었다. 원서는 F# 을 고를 다섯 가지 이유를 꼽은 다음 곧바로 개별 기능보다 맞물림이 중요하다고 덧붙였고(원서 pp.1-2), 책을 닫으면서 그 문장으로 돌아온다.`
  → 수정: `- 같은 이야기가 원서 서문에도 있다. 원서는 F# 을 고를 다섯 가지 이유를 꼽은 다음 곧바로 개별 기능보다 맞물림이 중요하다고 덧붙이고(원서 pp.1-2), 책을 닫으면서 그 문장으로 돌아온다.`
  두 가지다. (1) 한 문장 안에서 `덧붙였고` (과거)와 `돌아온다` (현재)가 섞였다.
  (2) 용어집 `preface` 행이 "원서 쪽을 가리킬 때는 원서 서문 으로 갈라 쓴다" 를 확정했고,
  같은 노트가 `서문 챕터` 를 노트 지칭으로 네 번 쓰므로 맨 "서문" 은 두 대상 사이에서 흔들린다.
  **인계 지시는 68·83줄을 그대로 두라고 했다.** 지칭 대상(원서 pp.1-2)은 바꾸지 않고
  표기만 용어집 확정형으로 맞추는 수정이므로 충돌하지 않는다고 판단했으나, 인계자가
  "손대지 말라" 는 뜻으로 지시한 것이라면 시제만 고치고 `서문` 은 남겨도 된다.
  83줄도 같다: `원서의 마지막 주장은 서문의 주장과 같다` → `원서의 마지막 주장은 원서 서문의 주장과 같다`.

- [16:69] 현재: `- 실제로 노트를 되짚어 보면 ... 14챕터의 API 핸들러는 거기에 10챕터의 클래스 타입과 4챕터의 컴파일 순서까지 얹힌다.`
  → 수정: `- 실제로 열다섯 챕터를 되짚어 보면 ... 14챕터의 API 핸들러에는 거기에 10챕터의 클래스 타입과 4챕터의 컴파일 순서까지 얹힌다.`
  (1) "노트" 단독은 이 노트 하나로 읽힌다. 이 노트에는 예제가 없으므로 앞선 열다섯 챕터를
  가리켜야 한다. 13줄 병합 교체문도 "열다섯 챕터의 실행 단위" 로 못박았다.
  (2) `얹히다` 는 피동이라 얹히는 것이 주어여야 한다. `핸들러는 … 얹힌다` 는 주술 불일치다.
  불릿 전체 최종안:
  ```
  - 실제로 열다섯 챕터를 되짚어 보면 한 챕터의 기능만으로 굴러가는 예제가 거의 없다. 8챕터의 검증은 3챕터의 `Result` 와 7챕터의 부분 액티브 패턴과 1챕터의 판별 유니온이 동시에 필요하고, 14챕터의 API 핸들러에는 거기에 10챕터의 클래스 타입과 4챕터의 컴파일 순서까지 얹힌다. 기능이 하나씩 늘 때마다 조합이 배로 늘어나는 것이 저자가 말하는 맞물림이다.
  ```

- [16:77] 현재: `- 규모를 더 키우기 전에 4챕터로 한 번 돌아가는 것도 값이 있다. ... 어떻게 자리를 잡는지 확인된다.`
  → 수정: `` - 규모를 더 키우기 전에 4챕터로 한 번 돌아가 보는 것도 해 볼 만하다. 13~15챕터의 예제에는 테스트가 없다. `HiveLog` 의 검증 함수와 저장소에 테스트를 붙여 보면 4챕터에서 배운 것이 실제 프로젝트에서 어떻게 자리를 잡는지 확인할 수 있다. ``
  ("값이 있다" 는 노트 18개에서 이 한 곳뿐인 표현이고 "가치가 있다" 의 변형으로도 굳지 않았다.
  "확인된다" 는 STYLE.md 가 막은 불필요한 피동이다.)

- [16:84] 현재: `여덟 항목 중 다섯은 지금도 살아 있는 것을 확인했고`
  → 수정: `여덟 항목 중 다섯은 지금도 살아 있음을 확인했고`
  ("살아 있는 것을 확인했다" 는 대상이 사물일 때 쓰는 꼴이다. 사실을 확인한 것이므로 명사형.)

## 구조

확인한 것부터 적는다.
- `##` 절 다섯 곳 모두 `(원서 p.NN)`·`(노트 보충)` 꼬리말이 있고, 문서 끝 대조 표의 절 이름·
  페이지와 글자까지 일치한다(Summary pp.192-193 / F# Software Foundation p.193 / Resources
  pp.193-194 / And Finally p.194 / 다음 걸음 (노트 보충)). 문서 제목의 pp.192-194 와도 맞는다.
  `## 정리` 를 대조 표에 넣지 않은 것은 12·15챕터와 같은 관례다.
- `### 열다섯 챕터에서 손에 남는 것` 에 페이지 꼬리말이 없는 것은 위반이 아니다. 노트가 스스로
  세운 소제목은 13챕터 671줄·14챕터 952줄·15챕터 709줄도 꼬리말 없이 적는다.
- 실행 단위 0개, 대조 표 실행 단위 칸 전부 `—`, `id` 불일치 없음. 코드 블록이 없으므로
  라벨 폭·열 정렬 문제도 없다.
- 인용 형식. 11줄 블록 인용에 `(원서 p.192, 저자의 신조를 옮김)` 이 달려 있고 한 문장이다.
  노트 18개 중 원문 인용 블록을 쓴 곳은 여기뿐이라 선례는 없으나 STYLE.md 요구(한 문장 이내,
  인용 표시와 페이지)를 그대로 지킨다. 67줄의 아리스토텔레스 문장은 산문에 녹여 옮겼고 절 제목이
  p.194 를 물고 있어 별도 페이지 표기가 필요 없다. 손댈 것 없다.

고칠 것.

- [16:5, 16:91] 병합 6.1 의 `200쪽 남짓` 은 뜻이 뒤집힌다 → `200쪽 가까이`
  "남짓" 은 "어느 한도에 차고 조금 남는 정도" 이므로 200쪽을 넘긴 쪽이다. 원문은 "just under
  200 pages" 로 못 미치는 쪽이다. 병합안을 그대로 넣으면 고치려던 사실이 반대로 틀린다.
  절 제목: `## Summary — 200쪽 가까이 지나간 자리 (원서 pp.192-193)`
  대조 표 행: `| Summary — 200쪽 가까이 지나간 자리 | pp.192-193 | — |`
  (두 곳을 반드시 함께 고쳐야 대조 표가 어긋나지 않는다.)

- [16:13] 13줄 최종안 (병합 16-6 대체)
  ```
  - 이 순서를 F# 에서 실행하는 도구가 F# Interactive(FSI) 다. 서문 챕터의 F# Interactive 절에 저자가 중단점을 걸어 본 일이 없다는 이야기가 있고(원서 p.6), 열다섯 챕터의 실행 단위 전부가 `dotnet fsi` 로 검증돼 있다. 먼저 돌아가는 것을 FSI 에서 확인하고, 그다음 파이프라인과 타입을 정리하고, 성능은 필요할 때 손대는 순서다.
  ```
  병합안과 다른 점은 두 가지다. 첫 등장 자리로 병기를 옮겼고, 절 이름의 `(FSI)` 를 떼어
  노트 안에 병기가 두 번 남지 않게 했다. 사실 서술(원서 p.6, `dotnet fsi` 검증)은 병합안 그대로다.

- [16:14] 14줄 — 병합 6.1 의 Giraffe 한 줄 보탬을 아래처럼 넣고, 색인이 하위 항목을 짚는다는
  한 문장을 함께 넣는다.
  왜 필요한가. 38줄 색인은 `커링`·`부분 적용`·`가드 절`·`Set`·`Async`·`Task` 를 "원서 목록의
  항목 중" 이라고 소개하는데, 14줄이 옮긴 목록에는 그 여섯이 없다(원서 목록의 하위 항목이고
  14줄은 최상위 15개만 옮겼다). 지금 상태로는 표와 색인이 서로 어긋난다.
  현재 불릿을 두 불릿으로 바꾼다.
  ```
  - 원서는 이어서 다룬 항목을 목차 형태의 목록으로 늘어놓는다. FSI, 대수적 타입 시스템, 패턴 매칭, `let` 바인딩, 불변성, 함수, `unit`, 효과, 계산 식, 컬렉션, 재귀, 객체 프로그래밍, 예외, 단위 테스트, 네임스페이스와 모듈이다. 몇몇 항목에는 하위 항목이 딸려 있다 — 함수 아래의 커링·부분 적용, 컬렉션 아래의 `Set` 이 그렇다. 아래 색인은 그 하위 항목까지 함께 짚는다.
  - 목록 뒤에는 독자가 Giraffe 로 API 와 웹사이트를 쓰는 여정도 시작했다는 한 줄이 붙는다. 아래 표의 13~15챕터가 그 자리다.
  ```
  병합 6.1 의 원문("원서는 목록 뒤에 Giraffe 로 API 와 웹사이트를 쓰는 여정도 시작했다는 한 줄을
  붙인다")은 안긴 문장의 주어가 `원서는` 으로 읽혀 원서가 여정을 시작한 것이 된다. 원문은
  "You've also started…" 이므로 주어를 `독자가` 로 세워야 한다. 또 같은 불릿이 `원서는` 으로
  시작하므로 한 불릿에 `원서는` 이 두 번 오는 것을 피해 문장을 갈랐다.

- [16:38] 38줄 색인 단락 최종안 (병합 16-1 대체)
  ```
  원서 목록의 항목 중 챕터 제목에 드러나지 않는 것들의 자리는 이렇다. FSI 는 서문 챕터의 F# Interactive 절, 대수적 타입 시스템은 1챕터, 불변성과 `let` 바인딩은 1챕터와 2챕터, `unit`·커링·부분 적용은 2챕터, 가드 절은 1·7·8챕터, `Set` 은 5챕터, 단위 테스트는 4챕터, 네임스페이스와 모듈은 4챕터와 9챕터다. 패턴 매칭은 1챕터가 잡고 7챕터가 넓힌다. 효과라는 이름은 12챕터가 붙이고, 그 아래의 `Option` 과 `Result` 는 3챕터, `Async` 와 `Task` 는 12챕터가 개념을 잡고 13~15챕터가 실습으로 쓴다. 원서 목록에 없지만 함께 찾게 되는 두 가지도 적어 둔다. 익명 함수는 2챕터에 있고, 가변 바인딩은 1챕터가 `<-` 와 함께 처음 다루고 10챕터가 클래스 안에 감춘 가변 상태로 다시 쓰며 13챕터의 Mutability 절이 설정 코드에서 쓰는 자리를 보여 준다.
  ```
  병합안에서 바꾼 것과 이유.
  1. `F# Interactive(FSI) 절` → `F# Interactive 절`. 병기는 13줄로 옮겼다(위 용어 항목).
  2. `예외는 3챕터,` 를 뺐다. 표의 3챕터 행 제목이 `` 3 `null` 과 예외 `` 이므로 이 단락이
     선언한 기준("챕터 제목에 드러나지 않는 것")에 어긋난다. 표와 색인이 부딪히는 자리다.
  3. `대수적 타입 시스템은 1챕터` 와 `효과 … `Option` 과 `Result` 는 3챕터` 를 넣었다.
     둘은 14줄 목록의 최상위 항목이면서 어느 챕터 제목에도 드러나지 않는데 병합안에서 빠져 있다.
     이대로면 색인이 자기 기준을 다 채우지 못한다. 근거는 1챕터 29줄(대수적 타입 시스템 첫 등장·
     병기), 12챕터 364줄과 용어집 `effect` 행(원서 p.153 이 이 이름을 붙인다), 표의 3챕터 행
     (`Result`·`Option`)이다. **챕터 배정 자체는 기술 검수 소관이므로 집필자가 확인해 확정하라.**
  4. 나열 방식을 갈랐다. `X 는 N챕터` 꼴 여덟 개를 첫 문장에 모으고, 동사가 붙는 항목
     (패턴 매칭·효과)은 뒤 문장으로 뺐다. 병합안은 두 꼴이 한 문장에 섞여 있어 목록이
     어디서 끊기는지 보이지 않는다.
  5. `1챕터와 7챕터와 8챕터` → `1·7·8챕터`. `와` 를 두 번 겹치지 않는다.

- [16:76] 불릿을 둘로 나눈다. 병합 16-2 교체문은 아홉 문장 한 덩어리로, 이 노트의 다른 불릿
  (3~4문장)보다 두 배 이상 길다. "왜 지금 바꿀 수 있는가" 와 "바꾸면 무엇이 달라지는가" 로
  끊으면 각 불릿이 이 노트의 평균 길이가 된다. 최종안은 아래 "선행 검수 지시서의 문제" 에 있다.

- [16:40, 16:65] (권장, 표 두 곳 동시 수정) 영어 절 제목의 한국어 부제가 두 곳만 없다.
  12·15챕터는 영어 절 제목마다 `— 한국어 부제` 를 붙인다(`Further Reading — 더 읽을 것`,
  `Summary — 원서의 챕터 요약`). 이 노트도 Summary·Resources 에는 붙였는데 나머지 둘에 없다.
  붙이려면 절 제목과 대조 표 행을 함께 고쳐야 한다.
  `## F# Software Foundation (원서 p.193)` → `## F# Software Foundation — 가입 권유 (원서 p.193)`
  `| F# Software Foundation | p.193 | — |` → `| F# Software Foundation — 가입 권유 | p.193 | — |`
  `## And Finally (원서 p.194)` → `## And Finally — 맺음말 한마디 (원서 p.194)`
  `| And Finally | p.194 | — |` → `| And Finally — 맺음말 한마디 | p.194 | — |`
  네 곳을 다 고치거나 넷 다 그대로 두어라. 절반만 고치면 대조 표가 어긋난다.

## 표와 색인의 문장 품질

표 15행의 문형은 "판단 기준" 으로 일관돼 있다. 각 행이 `…한다` 로 시작해 `…안다 / 구분한다 /
고른다` 로 닫히고, 숨은 주어가 모두 독자다. 병합 교체문 세 개를 넣은 뒤에도 이 틀이 유지되는지
확인했고, 두 곳이 어긋난다.

- [16:35] 14챕터 행 최종안 (병합 16-4 대체)
  ```
  | 14 Giraffe API | 읽기와 쓰기 경로를 나누고 상태 코드를 정한다. 요청 본문은 모델 바인딩으로 받고 그 뒤에 검증을 세운다. 저장소는 컨테이너에 싱글턴으로 등록해 핸들러에서 꺼내 쓴다 |
  ```
  병합안은 한 문장 안에서 숨은 주어가 독자에서 `핸들러가` 로 옮겨 간다(`…검증을 세우며,
  저장소는 … 핸들러가 꺼내 쓴다`). 다른 열네 행은 끝까지 독자가 주어다. 문장을 셋으로 끊고
  `핸들러가` → `핸들러에서` 로 바꾸면 주어가 독자로 유지되고, 사실 관계(요청 본문·검증 순서·
  싱글턴 등록·서비스 로케이션)는 병합안과 같다. 길이는 225바이트로 병합안과 같고 표에서 가장 긴
  1챕터 행(226바이트)과 나란하다.

- [16:36] 15챕터 행 최종안 (병합 16-5 대체)
  ```
  | 15 Giraffe 웹 페이지 | 뷰가 함수라는 점을 살려 레이아웃과 부분 뷰를 함수로 뽑아 조립한다. 정적 파일과 미들웨어 순서를 다룬다 |
  ```
  병합안 `뷰가 함수라는 점에서 … 함수로 뽑아` 는 한 문장에 "함수" 가 세 번 나오고, `~라는
  점에서` 는 이유를 대는 자리에 쓰는 꼴이라 `~한다` 와 잘 붙지 않는다. `점을 살려` 로 바꾸면
  둘 다 풀린다. "함수 합성" 을 쓰지 않는다는 병합 결정은 그대로다.

- [16:34] 13챕터 행은 병합 6.1 안(`라우팅이 값이고 핸들러가 함수라는 전제로 …`)을 그대로 쓰면
  된다. 문형이 다른 행과 어긋나지 않는다.

## 선행 검수 지시서의 문제

- [병합 6.1 / 16:5·16:91] `200쪽 남짓` — 뜻이 반대다. 위 "구조" 항목의 `200쪽 가까이` 를 쓰라.

- [병합 6.1 / 16:8] 병합안: `언어 기능 목록이나 패러다임 이름만의 차이가 아니라고 적는다`
  → 수정형: `언어 기능 목록이나 패러다임 이름의 차이만은 아니라고 적는다`
  ("이름만의 차이" 는 `만` 이 명사에 붙어 관형격 `의` 를 다시 받는 꼴로 어색하다. 보조사를
  서술어 쪽으로 옮기면 원문 "not just about …" 의 뜻이 그대로 살고 문장이 읽힌다.)

- [병합 6.1 / 16:48] 병합안: `표 아래 설명에서 두 곳만 보탠다.`
  → 수정형: `표 아래 설명에서 두 가지만 보탠다.`
  (보태는 것은 자리가 아니라 자료다. 61줄이 이미 "두 곳" 을 GitHub 조직·YouTube 채널이라는
  실제 두 자리를 세는 데 쓰고 있어 같은 절에서 뜻이 겹친다.)
  절 서두 최종안: `확인 시점은 2026년 9월이고, 표에는 원서에 없는 자료를 더하지 않았다. 표 아래 설명에서 두 가지만 보탠다.`
  범위 한정은 옳다. 62줄(Microsoft Learn)과 63줄(SAFE Stack)이 그 "두 가지" 이고, 아래
  Domain Modelling 불릿은 자료를 보태는 것이 아니므로 이 셈에 들지 않는다.

- [병합 6.1 / 16:52 아래] Domain Modelling 제목 차이 불릿은 문면 그대로 쓸 수 있다. 자리는
  62줄 뒤·63줄 앞이다. 그러면 표를 짚는 설명(61·62·새 불릿)이 먼저 오고 표에 없는 자료를
  보태는 불릿(63)이 마지막에 온다. 63줄 뒤에 두면 48줄이 말한 "두 가지" 와 뒤섞여 읽힌다.

- [16-expert.md 개선 권장 마지막 항목 / 16:73 뒤] 계산 식 갈래 불릿 — 세 곳을 고쳐야 한다.
  전문가안: `계산 식을 더 파고 싶으면 12챕터의 Further Reading 절이 가리키는 두 방향이 있다.
  계산 식으로 도메인 특화 언어를 짜는 Saturn 과 Farmer, 그리고 Async 와 Task 의 관계·취소·
  병행 실행을 다루는 Microsoft Learn 의 비동기 프로그래밍 문서다.`
  → 수정형:
  ```
  - 계산 식을 더 파고 싶으면 12챕터의 Further Reading 절이 가리키는 두 방향이 있다. 계산 식으로 도메인 특화 언어(DSL, Domain-Specific Language)를 짜는 Saturn 과 Farmer, 그리고 `Async` 와 `Task` 의 관계·취소·병행 실행을 다루는 Microsoft Learn 의 비동기 프로그래밍 문서다.
  ```
  (1) `Async`·`Task` 에 백틱이 없다. 타입 이름이므로 용어집 원칙대로 백틱을 살린다.
  (2) `도메인 특화 언어` 는 이 노트 첫 등장이다. 용어집 `Domain-Specific Language (DSL)` 행이
  "첫 등장에서 `도메인 특화 언어(DSL, Domain-Specific Language)` 로 한 번만 병기" 를 확정했다.
  (3) `병행 실행`·`취소` 는 12챕터 본문과 같은 낱말이므로 그대로 두었다.

- [병합 6.1 / 16:73] 병합·전문가가 손대지 않았으나 이 불릿은 열거 종결이 어긋난다.
  현재: `- 원서가 남긴 갈래는 세 개다. 첫째, … 지점(원서 p.164). 둘째, … 권유(원서 p.191). 셋째, 이 챕터의 자료 목록이다.`
  → 수정: `- 원서가 남긴 갈래는 셋이다. 첫째는 12챕터가 계산 식을 더 파려면 다른 자료로 넘어가라고 적어 둔 지점이다(원서 p.164). 둘째는 15챕터가 View Engine 은 겉만 훑었다며 문서를 읽어 보라고 한 지점, 그리고 JavaScript 를 직접 쓰기 싫으면 SAFE Stack 을 보라는 권유다(원서 p.191). 셋째는 이 챕터의 자료 목록이다.`
  (첫째·둘째는 종결어미가 없는 명사구인데 셋째만 `-이다` 로 닫힌다. 셋을 같은 꼴로 맞췄다.
  같은 절에서 `책 둘, 사이트 다섯` 처럼 세는 노트 관례에 맞춰 `세 개다` → `셋이다`.)

- [병합 16-2 / 16:76] 최종안 (병합 교체문을 두 불릿으로 대체)
  ```
  - 그 작업이 이 시점에 맞는 이유는 저장소의 모양에 있다. `InspectionStore` 는 밖으로 멤버 넷만 열어 두고 안쪽 사전을 감춘 클래스다(10챕터의 캡슐화). 핸들러는 그 인스턴스를 컨테이너에서 꺼내 쓴다(14챕터의 서비스 로케이션). 그래서 멤버의 시그니처가 그대로라면 안쪽 구현을 SQL 이든 문서 데이터베이스든 다른 것으로 바꿔도 핸들러에는 손댈 곳이 없다.
  - 다만 실제 데이터베이스는 호출이 비동기이고 실패할 수 있으므로 시그니처가 그대로 남지 않는다. `TryFind` 의 반환 타입이 `Inspection option` 에서 `Task<Inspection option>` 이나 `Task<Result<Inspection, DbError>>` 로 바뀐다. 핸들러는 이미 `task { ... }` 안에서 돌고 있으니 껍데기가 새로 필요한 것은 아니고, 저장소를 부르는 줄에 `let!` 이 붙는다. 실패까지 담기면 효과가 둘 겹치므로 12챕터의 복합 계산 식 자리가 여기다. `Async<Result<...>>` 에는 12챕터에서 쓴 `asyncResult`, `Task<Result<...>>` 에는 같은 패키지(`FsToolkit.ErrorHandling`)의 `taskResult` 를 쓴다. 구현을 둘 이상 두고 갈아 끼우려면 저장소를 인터페이스로 뽑는 일이 먼저인데, 14챕터가 그 일을 남겨 두었다.
  ```
  병합안에서 고친 것은 넷이다.
  1. `그 작업이 왜 이 시점에 맞는지는 … 모양에 있다` 는 주술 불일치다(의문 명사절이 `있다` 의
     주어가 될 수 없다). `이 시점에 맞는 이유는 … 에 있다` 로 바꿨다.
  2. `핸들러는 손댈 곳이 없다` → `핸들러에는 손댈 곳이 없다`.
  3. `같은 패키지의 taskResult` 에 선행어가 없다. 패키지 이름을 괄호에 넣고 `asyncResult` 쪽에
     "12챕터에서 쓴" 을 붙여 두 계산 식이 같은 패키지에서 온다는 사실이 문서 안에서 닫히게 했다.
  4. `14챕터가 그 대목을 남겨 두었다` 의 "그 대목" 이 앞 문장의 무엇을 가리키는지 흐리다.
     `먼저인데, 14챕터가 그 일을 남겨 두었다` 로 이어 붙였다.
  기술 서술(멤버 넷, 사전, 서비스 로케이션, `TryFind`, `let!`, `taskResult`)은 병합안 그대로다.

- [병합 16-3 / 16:75] 최종안 (마지막 문장만 병합안과 다르다)
  ```
  세 챕터를 지난 뒤 이 앱의 상태는 두 곳에 있다. 점검 기록은 `ConcurrentDictionary` 를 감싼 저장소 클래스 안에, 벌통 목록은 `Domain.fs` 의 최상위 `hives` 리스트에 있다. 점검 기록은 요청이 바꿀 수 있는 데이터이고 벌통 목록은 표본 데이터이며, 둘 다 메모리에만 있어 프로세스가 끝나면 사라진다.
  ```
  (병합안의 `앞은 … 뒤는 …` 은 같은 절 62줄이 같은 기능으로 `후자는` 을 쓰고 있어 한 문서에
  두 지시 방식이 생긴다. 이름을 다시 적으면 지시어가 아예 필요 없다.)

- [병합 16-8 / 16:85] 최종안 (조사 한 곳만 병합안과 다르다)
  ```
  - 실습을 이어 갈 자리는 `HiveLog` 의 저장소 뒤에 실제 데이터베이스를 붙이는 일이다. 캡슐화와 서비스 로케이션 덕에 멤버의 시그니처가 그대로면 핸들러에 손댈 곳이 없고, 반환 타입이 비동기와 실패를 담게 되면 그 호출 줄에 `let!` 이 붙으면서 12챕터의 복합 계산 식이 필요해진다.
  ```

- [16-expert.md 84-101행] 전문가의 `서문 노트` 확정과 파일 이름 병기는 병합 2.1 이 실측으로
  기각했다(`N챕터` 320회 대 파일 이름 병기 1곳). 13·18·38·43줄의 `서문 챕터` 는 그대로 둔다.
  전문가안을 그대로 넣지 마라. 전문가가 지시한 18줄 수정은 필요가 없어졌다(이 지시서는 18줄의
  시제만 고친다). 43줄은 병합 16-9 대로 페이지가 pp.4-5 다.

## 용어집 후보 (보고만 — 본문 수정 없음)

노트에 쓰이는데 용어집에 표제어가 없는 표기 셋이다. 표기 자체는 챕터 간에 일관되므로 본문을
고칠 곳은 없다. 다음 배치에서 F# 전문가가 등재를 판단하면 된다.

- `invariant` → 불변식. 10챕터 374줄이 `불변식(invariant)` 로 병기해 도입하고 726줄과
  16챕터 31줄이 병기 없이 쓴다. 계열은 기확정 `encapsulation`(캡슐화)과 붙는다.
- `dictionary` → 사전. 14챕터 144·288·790줄이 `ConcurrentDictionary` 를 "사전" 으로 부르고
  병합 16-2 교체문이 16챕터에 같은 낱말을 들인다.
- `sample data` → 표본 데이터. 15챕터 절 제목(`Loading Data on Startup — 표본 데이터`)과
  병합 16-3 교체문이 함께 쓴다.
